# 分层森林图详细规则（编写者手册）

> 读者：架构图的**编写者**（人 / agent）。**阅读者**读 `CLAUDE.md` §项目架构 的简略内容即可。
>
> 校验命令：`python3 scripts/format_architecture_blocks.py --check` 与 `python3 scripts/check_architecture_paths.py`。

## 1. 心智模型

**图是一张分层 DAG：节点可以被多条边共享引用。**

### 1.1 两类节点

| 类别 | 定义 | 图上形态 |
| --- | --- | --- |
| **抽象节点** | 只是一种概念或架构层 | 层名行 |
| **实体节点** | 可以定位到文件或文件夹，或者具体的一个数据 | 目录项行 / 值行 |

### 1.2 两类边

| 类别 | 连接 | 图上形态 |
| --- | --- | --- |
| **组成目录树的边** | 抽象节点 → 其所辖的实体节点 | 缩进 + 盒线（`├──` / `└──`） |
| **数据流边** | 实体节点 → 实体节点 | `↓` |

**目前不存在第三种边。**

### 1.3 书写模型

节点按 **充要定义一行 / 精确实现一行 / 契约一行或多行** 书写。

### 1.4 画图前提

```text
可以画出架构图 ⟺ 层间数据线性化流动 ∧ 层与实际目录对应 ∧ 契约可判定真假 ∧ 列出的文件与文件夹可被明确定义
```

### 1.5 不合并

即便两条边传递的是同样的数据，也**不合并**；按 DAG 的绘制方式类比绘制数据流边。

> 这条规则的**确切范围待定**（§7.1-1）。

### 1.6 来源可追

任一列的来源，要么是紧邻其上的层，要么被 `§` 列锚点声明（§5.3）。

## 2. 节点三元组

### 2.1 `≝` 充要定义

充要定义用符号 `≝` 表达，用自然语言书写。

### 2.2 `= ⊂ ⊃` 精确实现

精确化实现用集合语言并以集合关系符 `= ⊂ ⊃` 连接充要定义集：

| 符号 | 含义 |
| --- | --- |
| `=` | 定义和实现一致 |
| `⊂` | 定义不包含实现 |
| `⊃` | 实现落后于定义 |

- 若符号顺接层级定义，那么运算符右侧为**基目录**（相对仓库根的目录路径）。
- 多个基目录以 ` + ` 连接（但不建议使用这条规则，优先考虑重构）。
- **顶层各个架构层**默认**代码文件目录即精确实现的集合，故顶层不写 `=`**，但写定义行和契约行。
- **`=` 的判据**：目录项条目集 = 该目录的实现集，才写 `=`。
- **省略约定**：`*.test.ts`、`AGENTS.md`、资源目录（如 `frontend/src/assets/`）不入图，且必须显式声明——否则 `=` 的真值不可判定。

### 2.3 `s.t.` 契约

契约用 `s.t.` 引出**命题逻辑**，并用逻辑联结词（`∨ ∧ ¬ ⇒ ⟺`）构造由自然语言表达的各个简单命题，履行契约对应命题为真，违反契约对应命题为假。

- `∨ ∧ ¬` 与数学上的用法一致。
- `⇒` 理解为：只要不是“前件真，后件假”就为真。
- `⟺` 理解为：只要不是“两侧真值不同”就为真（即 `A ⇒ B ∧ B ⇒ A`）。

### 2.4 成员枚举 `= { … }`

- 用于列出该条目的公开面（如 `graph_store.ts` 的公开入口集合）。
- 要求：穷尽该条目的公开面；目录名不要混进函数名列表（会让人误以为两者同类）。

## 3. 数据流块

### 3.1 层与值交替

数据流由**层**（黑盒行）与**值**（数据行）交替构成，行间以 `↓` 连接。

- 层是**抽象节点**；值是**实体节点**（具体的一个数据）。

### 3.2 `↓` 是数据流边

- `↓` 独占一行，写 `↓ [函数]`：上面是输入或层，下面是输出或层，右边是转换动作，可留空。
- 层为黑盒：`≝` / `=` / `s.t.` 只声明接口；层上方的 `↓` 是其入口，下方的 `↓` 是其出口。

### 3.3 多步转换

同一列**允许**连续多行 `↓`（一次转换占一条边，多步就多行），但**各行之间必须上下对齐**。

### 3.4 多列 = 并行管道

多条 `↓` 按行、列排布：**列 = 一条管道**，同行并列 = 并行管道；读序行自上而下、行内自左而右。

## 4. 层级即缩进

缩进 = 层级，每级 4 空格：根 `0` / 层与流 `4` / 层属性 `8` / 目录项与续行 `12`

- 层间流（`↓` / 值 / `§` 引用）与层名同级，**不缩进进层内**。
- 目录项行：缩进 12，以 `├──` 起头、**末项用 `└──`**；它是所在抽象节点所辖的**实体节点**，与抽象节点之间由**组成目录树的边**相连。
- 目录项续行对齐到该文件的 `≝`；语义续行以 `│` 引导，纯对齐可省 `│`。

## 5. 其他符号

### 5.1 `§` 引用：语法

```text
§<层>: <实体节点>
```

- `<层>`：已定义的**抽象节点**，须与图内层名逐字一致。
- `<实体节点>`：**可省略**；指向该层下的实体节点（文件 / 文件夹 / 具体数据）。
- 同一层的多个实体节点并列：`§<层>: <实体节点1> + <实体节点2> + ...`（以 ` + ` 连接）。

### 5.2 用法一：整行引用

架构图中同一层重复出现（数据流回经该层）时写 `§<层>`（可带 `<实体节点>`），不再重复 `≝` / `=` / 注解。

反馈（逆层序）把目标层重锚到下方，保持输入在上、输出在下。

### 5.3 用法二：列级来源锚点

当某列的来源**不是紧邻其上的层**时，在该列 `↓` 的正上方写锚点行
例如：

```text
                         §工具交互逻辑层: cognition/deconstruct.ts
    ↓                    ↓
    registry + batch     operation params
    ↓ `apply_batches`    ↓ `compose`
```

- 作用域：**仅该列**。
- 对齐：锚点列位 = 该列 `↓` 的列位。
- 未加锚点的列沿用上方层（默认下行）。
- 锚定行其余列留空；锚点不替代该列的 `↓`（见 §7.1-3）。

### 5.4 `←` 注解

行末右侧 `←` 表注解。

## 6. 排版

- `↓` 多列块里**仅使用英文字符**，不要使用汉字，除非汉字处于行末，且之后没有 `↓` 需要对齐。
- `↓` 多列块里的**代码用 `` ` `` 包裹**，以区分自然语言英语。
- 同一列各行上下对齐（§3.3）；多列块各列的起点由该块内各段的最大宽度决定。

### 6.1 脚本盲区（必须人眼保证）

- `s.t.` 命题的真值。
- `§` 的 `<层>` 拼写、`<实体节点>` 是否存在。
- 锚点的列位正确性（放错也不会报错）。
- 条目是否**穷尽**了该目录的实现：脚本只查"列出的存在"，不查"未列的缺失"。
- 多列块字符集（"仅使用英文字符"）无校验。
- 目录项续行的对齐列由脚本按**整块**计算——正文"对齐到该文件的 `≝`"是简写（块内所有条目与续行共用一列）。
- 是否违背层间单向依赖契约。

## 7. 待定事项

### 7.1 规则待定

| # | 事项 | 现状 |
| --- | --- | --- |
| 1 | **"不合并"的确切范围**（只管"同一份数据被多个下游消费"，还是也管"同列多步"） | 未定；本文件只记原则（§1.5）与"多步各占一行"（§3.3） |
| 2 | **跨层的多来源列**怎么写：`§<层>: A + B` 只覆盖同层；工具层与 Runtime 层的两个来源如何并列 | 未定 |
| 3 | **锚定行的形式细节**：其余列留空是否必需、锚点是否必须与裸 `↓` 共存 | 现按示例形态执行，待确认 |
| 4 | **顶层不写 `=`** 与现图 4 处顶层 `=`（`CLAUDE.md:153`、`:172`、`:231`、`:255`）的关系 | 未定 |
| 5 | **反馈重锚的措辞**："数据流回经该层"与现状（前向汇入已定义层）是否吻合 | 未定 |
| 6 | `↓ [函数]` 的方括号写法在图内**零使用** | 未定：视为元语法占位符，还是要求字面书写 |

### 7.2 图内待迁移

| 位置 | 现状 | 目标 |
| --- | --- | --- |
| `CLAUDE.md:224` | `§工具交互逻辑层 / ui 编排层`：`/` 不是本语法中的分隔符，且 `ui 编排层` 不是层名 | 工具层部分写 `§工具交互逻辑层: <实体节点>`；`ui/operation_controller.ts` 是 **Runtime 的目录项**，按 §7.1-2 处理 |
| `CLAUDE.md:260` | `§工具交互逻辑层`（省略了实体节点） | 合法（§5.1）；若要指明调用者，用 ` + ` 并列多个实体节点 |
| `CLAUDE.md:247` | `§Runtime 状态与图业务层` | 合法，无需迁移 |
| `CLAUDE.md:265` | `§Cytoscape 渲染与交互层` | 合法，无需迁移 |

`§` 相关改动**不受任何脚本保护**，改完请跑两条校验命令。

### 7.3 工具缺陷（事实，待处理）

| 缺陷 | 影响 | 绕法 |
| --- | --- | --- |
| 两个脚本都只处理文档**第一个** ` ```text ` 块 | 一份 md 放多张图时，第二张起完全失检 | 一份 md 只放一张架构图 |
| `--write` 把缩进 ≥ 16 的条目行当续行收编（前提：该行与缩进 12 的条目行**同块连续**） | 深层级被静默抹平（`--check` 会报出，只有 `--write` 静默改写） | 条目深度只到 12，深层结构用内联集合 |
| 多基目录的公共前缀为空时 `os.path.commonpath` 返回 `''` | 候选退化为绝对路径，可能**假通过** | 避免 ` + ` 多基目录；必须用时人工核对 |
| 基目录混用绝对路径与相对路径时 `os.path.commonpath` 抛 `ValueError` | 脚本异常退出，不给失效清单 | 基目录一律写仓库根相对路径 |
| `check_architecture_paths.py` 不在 `CLAUDE.md` 命令清单里、无 CI | 容易忘记跑 | 改完必跑两条校验命令 |
