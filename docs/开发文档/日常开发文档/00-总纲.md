# 总纲

## 开发步骤

| # | 文件 | 内容 | 状态 |
| --- | --- | --- | --- |
| 01 | [01-重命名图谱](步骤/01-重命名图谱.md) | GE 端 update_graph 图级操作（类型 / 校验 / 兑现 / 逆元 / 批级契约）；前端导航卡片接入（更名按钮 + 行内编辑 + renameRootGraph 用例） | ✅ |
| 02 | [02-持久化层抽取与图数据迁移](步骤/02-持久化层抽取与图数据迁移.md) | 抽出 `frontend/src/persistence/`（唯一入口 + 跨类别协调 + 数据类别 + 介质 四条目）；迁移 18 个调用点；写失败可见化；介质不静默降级 · commit `367b0ec` | ✅ |
| 03 | [03-图数据导出](步骤/03-图数据导出.md) | 按根图树导出为 JSON 文件（`{ format: 1, graphs }`）；导航面板底部「导出 / 导入」入口（导入为 disabled 占位）；置顶选择弹窗 · commit `367b0ec` | ✅ |
| 04 | [04-视图状态收编](步骤/04-视图状态收编.md) | 视图状态键合并与收编导航卡片位置，让「唯一入口」成为可 grep 验证的事实 | ⏳ |
| 05 | [05-禁止跨树引用](步骤/05-禁止跨树引用.md) | 引擎新增森林级注册表不变量（引用必须与源同树）+ `diverge` peer 域收窄到本树 + 树收集收敛为纯算法 + dev 种子同树；消解 03 `U-3` · commit `617c777` | ✅ |
| 06 | [06-统一写入口与树级校验](步骤/06-统一写入口与树级校验.md) | （待规划）统一写入口（`deleteGraphTree` / `findParentGraph`）+ 树级校验（结构完整性、树内引用不悬空）+ 启动全量校验 | ⏳ |
| 07 | [07-校验判据与校验归位](步骤/07-校验判据与校验归位.md) | （待规划）确立「硬 ⟺ 被消费者依赖」判据；据此重审前置校验的去留：保留 / 拆分到调用方 / 下沉为一致的不变量校验；`skipValidate` 退役 | ⏳ |
| 08 | [08-持久化P2-IndexedDB与真原子性](步骤/08-持久化P2-IndexedDB与真原子性.md) | （待规划）**持久化 P2**：`medium/indexed_db.ts` + 介质策略点；图数据与日志切 IndexedDB（视图状态留 localStorage）；公开签名异步化；消灭半写态 | ⏳ |
| 09 | [09-持久化P3-操作日志持久化](步骤/09-持久化P3-操作日志持久化.md) | （待规划）**持久化 P3**：`categories/operation_log.ts`（键 `oplog:<rootGraphId>`）；切根图树由「重置日志」改为「加载该树的日志」；`coordination/` 加日志分支；收尾额外产出持久化层**真实子架构图** | ⏳ |
