# 051-1 — GE 导出碰撞检测：提示词

> 拓展自：[051-预览层搭建.md](../../步骤/051-预览层搭建.md) §5.1

## 当前状态

`hasCollisionAt` 函数已存在于 `packages/graph-engine/src/infrastructure/collision.ts`，功能完整、测试覆盖。但引擎 `index.ts` 第 19 行规则声明 `collision / placement / geometry 不导出`，前端无法直接 import。

预览引擎需要直接调用 `hasCollisionAt` 做预判（而非通过 compose 函数间接使用），需打破此导出规则。

## 具体子任务

### 1. 导出 hasCollisionAt

在 `packages/graph-engine/src/index.ts` 的 infrastructure 导出区块中，新增一行 export。

**规则**：
- 只导出 `hasCollisionAt`，不导出 `hasCollisionInDrafts`
- 放置位置：与其他 infrastructure 导出并列（`searchNodes` 附近）
- 注释：说明消费者为前端 preview_engine（预览碰撞预判）

## 新增/修改文件

| 文件 | 职责 | 操作 |
|------|------|------|
| `packages/graph-engine/src/index.ts` | 公开 API 入口 | 修改（+1 export） |

## 变更边界

**禁止修改**：
- `packages/graph-engine/src/infrastructure/collision.ts` — 函数本身不变
- 其他任何引擎文件

## 验收标准

- [ ] `import { hasCollisionAt } from '@my-project/graph-engine'` 在前端可用
- [ ] `pnpm --filter @my-project/graph-engine test` 全部通过
- [ ] TS 编译无错误

## subagent task 返回要求

返回改动的 diff 摘要；确认 `hasCollisionAt` 可在前端 import。
