# Phase 2b — Step 050：渲染/交互层重构

> 严格去 Cy 耦合的架构调整。背景：3.0-4 的 move_node 拾取放置暴露了"Cy 只是渲染层"规则的临时例外——工具直接 import `getCyInstance()` 操作 Cy 实例，渲染层边界模糊。本轮重构建立明确的渲染/交互层封装边界。

**前置来源**：[05-步骤-共依赖.md](05-步骤-共依赖.md) §3.0-5

---

## 已确认决策

1. **Cy 的角色重新定位为"交互层 + 渲染层"**（不再单称"渲染层"）。
   - Cy 实际承担两件事——`cy.json()` + stylesheet 负责视觉渲染，`tap`/`cxttap`/`dblclick` 事件负责用户手势捕获。

2. **目录重命名**：`frontend/src/render/` → `frontend/src/cytoscape/`。表达"此目录是 Cy 外部库的严格隔离层"的架构意图——所有直接依赖 `cytoscape` 的代码集中在此目录，目录外不可见。

3. **术语替换**：后续开发中不再使用"投影"（`mapNodeToCyElement` 的字段挑选本质上不是投影），替代为：
   - **映射**（mapping）：GraphData 字段 → CSS class 的语义转换（如 `role + kind → ['node-knowledge', 'node-atomic']`）
   - **拷贝**（copy）：GraphData 字段原样写入 Cy `data`

4. **Cy 的 `data` 仅存放渲染所需的最小字段**：`id`、`label`。现有 `CyNodeData` 中的 `role`、`kind`、`form`、`degree`、`abstractionLevel` 在 `getNodeClasses` 消费完后不再进入 Cy `data`。

5. **严格去耦合**：`cytoscape/` 目录之外禁止 `import` 以下任一：
   - `getCyInstance()` / `getInstance()`（已删除）
   - `import cytoscape from 'cytoscape'`
   - `import type { Core } from 'cytoscape'`

6. **renderer 对外暴露语义方法**：工具通过 `renderer.setNodePosition()` / `renderer.addNodeClass()` 等高层方法操作视觉，不直接接触 Cy 实例。

7. **syncFromGraphData 时清除全部 transient 状态**：防御性设计——将"操作期间不改 GraphData"的隐式契约变为显式约束。

8. **高亮走反应式逻辑**：renderer 暴露 `bindHighlight(getter, className)`，内部用 `watch(getter)` 自动管理 class 切换。

9. **语义方法以模块级函数导出**：`import { setNodePosition, addNodeClass, ... } from '@/cytoscape/useRenderer'`。延续当前 `getCyInstance` 的模式（模块级内部访问 `cyInstance`），但包裹为语义方法名，不暴露裸实例。

10. **`resetNodePosition` 作为独立 API**：renderer 在每次 `syncFromGraphData` 时记录每个节点的 GraphData 位置。`resetNodePosition(id)` 将节点位置恢复到该记录值。工具无需自行保存 `originalPosition`。

11. **`clearAllPreviews(owner)` 仅管理 class**：不重置位置。位置由工具自行调 `setNodePosition` / `resetNodePosition` 管理，防止语义污染。

---

## 架构总览（调整后）

```
cytoscape/
  ├── useRenderer.ts   — 渲染生命周期 + 语义 API
  ├── graph_interaction.ts         — Cy 事件 → 语义事件（纯函数，非组合式）
  ├── graph_element_mapper.ts      — GraphData → Cy elements（独立纯函数）
  └── cytoscape_style.ts           — stylesheet

GraphData (唯一事实源)
    │
    ▼
graph_element_mapper.ts（纯函数映射 + 拷贝）
    │  GraphData 字段 → CSS class（语义映射）
    │  id / label / position / classes（最小拷贝）
    ▼
useRenderer.ts（渲染层）
    │  ┌ syncFromGraphData(graphData)     — 内部调 mapper + cy.json() + 清 transient
    │  ├ centerOnElement(id)                — 视口定位 + 闪烁（不变）
    │  ├ setNodePosition(id, pos)         — 视觉预览位置
    │  ├ addNodeClass / removeNodeClass   — 视觉预览样式（只走 stylesheet）
    │  ├ bindHighlight(getter, className) — 反应式外部高亮
    │  ├ trackCursor(callback)            — 光标追踪 → 模型坐标
    │  ├ clearAllPreviews(owner)          — deactivate 时清理
    │  └ 内部管理 transient 视觉状态（按 owner 命名空间隔离）
    │
    ▼
Cy 实例（仅 cytoscape/ 内部可见，外部零 import）
    │
    ├── 视觉渲染（cy.json + stylesheet）
    └── 事件捕获（tap/cxttap/dblclick — 通过 graph_interaction.ts 翻译）
            │
            ▼
graph_interaction.ts（纯函数 bindCyEvents）
    Cy 事件 → 语义事件（onNodeClick / onEdgeClick / onRightClick / onNodeDoubleClick）
            │
            ▼
mediator.ts（事件路由，不变）
    语义事件 → 活跃 handler
            │
            ▼
工具 handler（业务逻辑，不变）
    构造 operation → graphStore.applyBatch
    视觉预览需求 → renderer 语义方法
```

---

## Step 1：mapper 清理 + 目录迁移 + 样式补充（低风险）

难度：🟢/🟢/3处 — 纯拷贝搬迁 + 删字段 + 改名，行为零变更

### 子任务

| # | 内容 | 约束 |
|---|------|------|
| 1 | 目录重命名：`render/` → `cytoscape/`。更新所有 import 路径（约 ~10 处） | 使用 `git mv` 保留历史 |
| 2 | `graph_element_mapper.ts`：`CyNodeData` 仅保留 `id` / `label`；`CyEdgeData` 仅保留 `id` / `source` / `target` / `label`（移除 `role`、`kind`、`form`、`degree`、`abstractionLevel`、`direction`、`referenceKind`） | `getNodeClasses` / `getEdgeClasses` 内部仍消费这些字段，但不写入 Cy `data` |
| 3 | `graph_element_mapper.ts`：`getFoldedNodeIds` 和 `getFoldedParentNodeIds` 内联到 `mapGraphDataToCyElements` | 各只被调用一次，无需独立导出 |
| 4 | `graph_element_mapper.ts`：更新注释——"投影"术语替换为"映射"/"拷贝" | 仅注释，不改逻辑 |
| 5 | `cytoscape_style.ts`：新增 `move-picked` class（`opacity: 0.4`），替代 `move_node.ts` 中 `.style('opacity', 0.4)` | 硬编码 opacity 值进入 stylesheet |
| 6 | `graph_interaction.ts`：去掉 `use` 前缀，改为普通函数 `bindCyEvents(cy, handlers)`，返回 `{ destroy() }` | 无内部状态，非组合式函数 |

### Scope Guard

- 禁止修改 `useRenderer.ts`（Step 2 的工作）
- 禁止修改 `move_node.ts`（Step 2 的工作）
- 禁止修改 `Graph.vue`（Step 2 的工作）
- 禁止修改 `mediator.ts`、`graphStore`、`uiStore`
- 禁止修改 `cytoscape_style.ts` 中已有样式规则
- 禁止添加新依赖

### 验收标准

- [ ] `frontend/src/cytoscape/` 目录存在，4 个文件全部迁移
- [ ] 所有 import 路径更新完毕，无残留 `from '@/render/'` 引用
- [ ] `CyNodeData` 仅含 `id` / `label`；`CyEdgeData` 仅含 `id` / `source` / `target` / `label`
- [ ] `getFoldedNodeIds` / `getFoldedParentNodeIds` 不再 export
- [ ] `cytoscape_style.ts` 含 `move-picked` class
- [ ] `bindCyEvents` 为普通函数（非 `use` 前缀），返回 `{ destroy() }`
- [ ] `pnpm --filter frontend test` 全部通过
- [ ] 前端运行无 TS 编译错误

---

## Step 2：renderer API 重构 + move_node 迁移 + Graph.vue 清理（高风险）

难度：🟡/🟡/4处 — renderer 新增 ~100 行 API + move_node 迁移 ~20 处 + Graph.vue 清理 ~50 行

### 子任务

| # | 内容 | 约束 |
|---|------|------|
| 1 | `useRenderer.ts`：删除 `getCyInstance()` 导出；删除 `getInstance()` 返回 | 外部彻底无法获取裸 Cy 实例 |
| 2 | `useRenderer.ts`：新增 `syncFromGraphData(graphData)` — 内部调 `mapGraphDataToCyElements` + `cy.json()` + 清全部 transient | 替代旧 `syncElements(cyElements)` 外部调 mapper 模式 |
| 3 | `useRenderer.ts`：新增模块级语义方法 — `setNodePosition` / `getNodePosition` / `resetNodePosition` / `addNodeClass` / `removeNodeClass` / `clearAllPreviews` / `trackCursor` / `bindHighlight` | 方法以模块级函数导出（非 composable 内方法），工具直接 `import { ... } from '@/cytoscape/useRenderer'`；`addNodeClass` / `removeNodeClass` / `clearAllPreviews` 接受 `owner: string` 参数做命名空间隔离，**仅管理 class**（不操作 position）；内部维护 `Map<owner, Map<nodeId, Set<className>>>` 管理 transient 状态 |
| 4 | `useRenderer.ts`：`bindHighlight(getter, className)` 内部用 `watch(getter)` — 旧 ID 去 class，新 ID 加 class | 替代 Graph.vue 的 `watchPendingTarget` |
| 5 | `move_node.ts`：删除 `import { getCyInstance }`；删除所有 `cy.getElementById()` / `.position()` / `.style()` / `.addClass()` 调用 | 改为调 renderer 语义方法 |
| 6 | `move_node.ts`：`activate()` 时 `trackCursor(callback)`；`deactivate()` 时 `clearAllPreviews('move')` + `tracking.stop()` | 替代 DOM mousemove 自绑定 + 手动弹回清样式 |
| 7 | `Graph.vue`：删除 `import { mapGraphDataToCyElements }`；`renderer.syncElements(cyElements)` → `renderer.syncFromGraphData(graphStore.graphView)` | Graph.vue 不再知道 mapper 存在 |
| 8 | `Graph.vue`：删除 `watchPendingTarget` 辅助函数及对 `cy.getElementById().addClass()` 的所有直接调用；改为 `renderer.bindHighlight(...)` | 高亮逻辑归 renderer |
| 9 | `Graph.vue`：删除 `bindCyEvents` 的直接 import 和调用；改为在 `renderer.mount()` 时传入 handlers，renderer 内部完成事件绑定 | `getInstance()` 删除后 Graph.vue 无 Cy 实例可用；`graph_interaction.ts` 的 `bindCyEvents(cy, handlers)` 由 renderer 在 mount 时内部调用 |

### renderer 新增 API 详单

所有方法以**模块级函数**导出（`import { setNodePosition, ... } from '@/cytoscape/useRenderer'`），内部访问模块级 `cyInstance`（同当前 `getCyInstance` 模式但包裹语义名）。

```ts
// 数据同步
syncFromGraphData(graphData: GraphData): void

// 视口控制（不变）
centerOnElement(elementId: string): void

// 视觉预览：位置
setNodePosition(nodeId: string, pos: { x: number; y: number }): void
getNodePosition(nodeId: string): { x: number; y: number } | null
// 重置节点位置到最近一次 syncFromGraphData 时记录的 GraphData 位置
resetNodePosition(nodeId: string): void

// 视觉预览：样式 class（仅走 stylesheet，带 owner 命名空间；仅管理 class，不操作 position）
addNodeClass(nodeId: string, className: string, owner: string): void
removeNodeClass(nodeId: string, className: string, owner: string): void
clearAllPreviews(owner: string): void

// 反应式外部高亮（内部用 watch）
bindHighlight(getter: () => string | null | undefined, className: string): void

// 光标追踪（内部管理 DOM mousemove 绑定/解绑 + 坐标转换）
trackCursor(callback: (modelPos: { x: number; y: number }) => void): { stop(): void }
```

### Scope Guard

- 禁止修改 `mediator.ts`
- 禁止修改 `graphStore`、`uiStore`、`draft_store`
- 禁止修改 `GraphNodeWindow.vue`、`GraphNavigationCard.vue`
- 禁止修改 `toolbar/` 下除 `move_node.ts` 外的其他 handler
- 禁止修改 `feature-tools/types.ts`（ToolHandler 接口不变）
- 禁止导出 `getCyInstance` 或 `getInstance` 的替代品
- 禁止在 `cytoscape/` 之外新增 `import cytoscape` 或 `import type from 'cytoscape'`

### 验收标准

- [ ] `useRenderer.ts` 不再导出 `getCyInstance()` / `getInstance()`
- [ ] `move_node.ts` 不再 import 任何 cytoscape 类型或 `getCyInstance()`
- [ ] 拾取放置功能行为不变：点击拾取 → 节点跟随光标 → 点击放置 → 碰撞红色高亮 → 右键取消弹回
- [ ] delete 工具高亮正常（`delete-target` class 正常施加/移除）
- [ ] add-edge 工具起点高亮正常（`edge-source-target` class 正常施加/移除）
- [ ] 搜索定位闪烁正常（`centerOnElement` + `search-focus`）
- [ ] `Graph.vue` 不再 import `mapGraphDataToCyElements`，不直接调 `cy.getElementById()`
- [ ] `pnpm --filter frontend test` 全部通过
- [ ] `grep 'getCyInstance\|getInstance' frontend/src/ --include='*.ts' --include='*.vue' | grep -v cytoscape/` 返回空（除注释外的外部零引用）
- [ ] `grep "from 'cytoscape'" frontend/src/ --include='*.ts' --include='*.vue' | grep -v cytoscape/` 返回空
- [ ] 工具栏原有 9 工具 + deconstruct 功能不受影响

---

## 明日测试预留

Step 2 执行完毕后，`move_node.ts` 的视觉预览走 renderer 语义接口，不再直接依赖 Cy 实例。此时可编写：

- `move_node` 状态机单测：mock renderer API（`setNodePosition` / `addNodeClass` / `trackCursor`）→ 验证 idle/picked 状态流转 + collision 逻辑
- 回归测试：确保拾取放置端到端行为不变

当前暂不编写。Step 2 完成后根据稳定情况决定优先级。

---

## 解决的问题对照

| # | 问题（来自05-步骤-共依赖.md §前置发现） | Step 1 | Step 2 |
|---|--------------------------------------|--------|--------|
| 1 | syncElements 静默覆盖 | — | ✅ syncFromGraphData 清 transient |
| 2 | 不可单测 | — | ✅ 工具不碰 Cy，renderer API 可 mock |
| 3 | Cy 查找样板重复 | — | ✅ 封装在 renderer 语义方法内 |
| 4 | 无强制清理契约 | — | ✅ clearAllPreviews(owner) |
| 5 | 隐式第二条数据流 | — | ✅ renderer 是 transient 唯一所有者 |
| 6 | 样式源分裂 | ✅ move-picked class 入 stylesheet | — |
| 7 | Cy 实例访问路径分裂 | — | ✅ getCyInstance + getInstance 均删除 |
| 8 | 组合式函数定位 | ✅ graph_interaction 改普通函数 | — |
| 9 | CyNodeData 携带无用字段 | ✅ 精简为 id/label | — |
| 10 | getFoldedNodeIds 过度拆分 | ✅ 内联 | — |
| 11 | Graph.vue 中 add-edge 高亮 | — | ✅ bindHighlight 归 renderer |

---

## 子步骤依赖

```
Step 1（低风险）→ Step 2（高风险）→ 明日测试（预留）
```

Step 1 完成后即可以当前代码基线运行——mapper 清理 + 目录迁移不影响行为。Step 2 依赖 Step 1 的 `cytoscape/` 目录和 `bindCyEvents` 命名。
