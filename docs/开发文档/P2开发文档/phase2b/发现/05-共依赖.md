# Phase 2b — 发现：Step 3.-1 清理 graph_operations.ts

> 执行期间由 @oracle 审查发现，编排者复核后归档。

---

## 3.-1 清理 graph_operations.ts 中间层

### 改进：`// 清理` 杂散注释残留

位置：`frontend/src/ui/operation_controller.ts`（原 481, 490, 499, 543 行，已修复）

现象：fixer 执行后在四处 section 分隔注释下各留下一行 `// 清理`，非文档注释，无意义。

> 建议：删除即可。已修复。

状态：✅ 已确认（修复于同一会话）

### 改进：未使用的 `NodeData` / `EdgeData` import

位置：`frontend/src/ui/operation_controller.ts`（原第 35-36 行，已修复）

现象：`import type { NodeData, EdgeData }` 在 `operation_controller.ts` 全文件中未被任何函数签名或类型注解引用。原 import 来自 `graph_operations.ts` 搬迁时残留。

> 建议：删除即可。已修复。

状态：✅ 已确认（修复于同一会话）

---

## 3.0-1 统一激活框架

> 执行期间由 @oracle + @explorer 审查发现，编排者复核后归档。

### 改进：GraphModeSelector 中 induce/internalize 按钮未走 mediator

位置：`frontend/src/components/GraphModeSelector.vue`

现象：induce/internalize 按钮仍调用 `controller.induce([])`，而非 `mediator.activate('induce')`。deconstruct 按钮已正确走 mediator。两者当前功能等价（均 early-return），但风格不一致。

> 建议：后续实现对应 ToolHandler 时一并修正。无需单独处理。

状态：⏳ 待确认

### 信息：deconstruct.ts 中 `computed` 导入可选优化

位置：`frontend/src/feature-tools/cognition/deconstruct.ts`

现象：`cursorClass` 用 `computed` 包装，但仅通过 getter `.value` 取值，computed 缓存特性在此场景无收益。

> 建议：可选整洁度改进——改为 getter 内直接判断 `isActive.value`。

状态：⏳ 待确认

### 信息：`lastValidationResult` 冗余赋值——代码库范围问题

位置：所有 handler（deconstruct.ts、add-node.ts、add-edge.ts、delete.ts、fold.ts）+ operation_controller 中 compose 函数

现象：`graphStore.applyBatchToGraph()` 内部已设置 `lastValidationResult`（`graph_store.ts:572`），但每个调用者随后重复写入相同值。非 3.0-1 引入——deconstruct.ts 沿用了现有模式。

> 建议：代码库范围的独立清理任务，不在本步骤处理。

状态：⏳ 待确认

### 信息：compose 模式提取延期至 3.0-2

位置：`deconstruct.ts` + `operation_controller.ts` 中 induce/internalize/diverge/moveNode

现象：`compose → hasErrors → mapComposeIssues → applyBatch → setValidation` 五步模式在 5 处重复。步骤文档 3.0-2 已规划提取共享辅助函数。

> 建议：按计划在 3.0-2 执行。

状态：⏳ 待确认

---

## 3.0-2 operation_controller 职责收缩

> 执行期间由 @explorer（简化审查）+ @oracle（代码审查）发现，编排者复核后归档。

### 改进 I-1：`'default'` 不在 `ToolId` 联合类型中

位置：
- `frontend/src/feature-tools/types.ts:76` — `ToolId = OperationTool | CognitionTool | ArrangementTool`
- `frontend/src/views/Graph.vue:122` — `mediator.register('default' as any, ...)`
- `frontend/src/views/Graph.vue:160` — `activeToolId !== ('default' as any)`
- `frontend/src/feature-tools/mediator.ts:86,94,102` — `registry.get('default' as ToolId)`
- `frontend/src/feature-tools/default.ts:45` — `id: 'default' as ToolId`

现象：`'default'` 作为 mediator fallback 工具 ID 使用了 4 处类型断言绕过（1 处 `as any` + 3 处 `as ToolId`），`ToolId` 联合类型不包含它。scope guard 禁止修改 `types.ts`，故当前为最小侵入方案。

> 建议：在允许修改 `types.ts` 的步骤中，将 `'default'` 加入 `ToolId` 联合类型，移除所有类型断言。

状态：✅ 已确认（`'default'` 已加入 `ToolId`，`Graph.vue` 两处 `as any` 已移除）

### 改进 I-2：`Graph.vue:122` 与 `default.ts:45` 类型断言不一致

位置：`Graph.vue:122` 用 `as any`，`default.ts:45` 用 `as ToolId`

现象：同一语义的类型逃逸用了两种不同写法，不一致。`as ToolId` 比 `as any` 更精确。

> 建议：统一为 `as ToolId`。

状态：✅ 已确认（I-1 修复后 `as any` 已直接移除，无需断言）

### 改进 I-3：`nodes.find` 查找模式在 `default.ts` 与 `delete.ts` 中重复

位置：
- `frontend/src/feature-tools/default.ts:60` — `graphStore.graphView?.nodes.find(n => n.id === nodeId)`
- `frontend/src/feature-tools/toolbar/delete.ts:52` — 同样模式

现象：两处独立 handler 重复了同一条按 ID 查找节点的逻辑。按项目规范（≥2 次调用 → 拆辅助函数），可提取。

> 建议：在 `graph_store.ts` 或共享模块中增加 `findNodeById` 辅助函数，两处改为调用。

状态：✅ 已确认（延后处理）

### 改进 I-4：`mediator.test.ts` 缺少 default fallback 测试覆盖

位置：`frontend/src/feature-tools/mediator.test.ts`

现象：测试覆盖了 activate/deactivate/事件转发/右键取消，但 `mediator.ts:82-104` 的 else 分支（activeHandler 为 null 时 fallback 到 default）无测试。

> 建议：后续补充。

状态：✅ 已确认（延后处理）

### 不确定 U-1：`diverge` 和 `moveNode` 零消费者

位置：`frontend/src/ui/operation_controller.ts` — `diverge`（~L284-337）、`moveNode`（~L352-379）

现象：两个函数完整实现但无任何调用方。属于 compose 库存，等待各自步骤迁出到独立 ToolHandler。

状态：✅ 已确认（后续步骤有明确迁出计划）

### 不确定 U-2：`GraphModeSelector.vue` Induce / Internalize 按钮传空数组

位置：`frontend/src/components/GraphModeSelector.vue:51,54` — `controller.induce([])` / `controller.internalize([])`

现象：induce 在 `nodeIds.length < 2` 时提前返回，internalize 在 `nodeIds.length === 0` 时提前返回。按钮点击后静默 no-op。**历史遗留问题**，非本次改动引入。

状态：✅ 已确认（Phase 2b 多选 UI 任务覆盖）

### 不确定 U-3：`ui_types.ts` 删除

位置：`frontend/src/types/ui_types.ts`（已删除）

现象：`CognitionAction` / `ArrangementAction` 在 3.0-1 + 3.0-2 后无消费者残留。Grep 确认零引用。fixer 在本次改动中顺手清理。

状态：✅ 已确认（3.0-1 清理 ui_store + 3.0-2 删除 controller import → 文件成为死代码，有意清理）

---

## 3.0-3 default 工具扩展 — onConfirm

> 执行期间由 @oracle 审查发现，编排者复核后归档。

### 历史遗留 H1：浮空窗未编辑即 Confirm 静默无反馈

位置：`frontend/src/components/GraphNodeWindow.vue:185-186`

现象：`editingData` 仅在用户首次输入时才初始化。若用户打开浮空窗后未作任何修改就点 Confirm，`editingData` 为 null，函数静默返回，浮空窗不关闭也无提示。旧代码同样存在此问题，非本次引入。

> 建议：在此场景关闭浮空窗（无修改 = 无需写入），或至少给一个视觉反馈。

状态：✅ 已确认（暂不处理）状态：⏳ 待确认

### 改进 U1-已修复：工具切换时浮空窗 Confirm 静默失败

位置：`frontend/src/feature-tools/mediator.ts:70` — `activate()` 逻辑

现象：3.0-3 后浮空窗 Confirm 必须 default 工具活跃时才成功。若用户在浮空窗打开期间切换到其他工具，`mediator.activeHandler?.onConfirm?.()` 在非 default handler 上不会触发，Confirm 静默失败且浮空窗不关闭。

修复：`mediator.activate()` 中切换工具时自动调 `uiStore.closeFloatingWindow()`。切换工具 = 放弃当前浮空窗操作。

状态：✅ 已修复（于同一会话）

---

## 3.0-4 move_node + 拾取放置

> 执行期间由 @oracle 审查发现，编排者复核后归档。B1/B3/B4 已修复，B2/B5/B6 为观察/债务项。

### 历史遗留 H2：Graph.vue ToolNotification 渲染与 Delete 硬编码耦合

位置：`frontend/src/views/Graph.vue:421`

现象：ToolNotification 面板中硬编码 `"再次点击将删除："` 前缀。move handler 的碰撞 notification 消息为 `"节点在目标位置与已有节点碰撞，无法放置。"`，被此面板渲染时显示为 `"再次点击将删除：节点在目标位置与已有节点碰撞，无法放置。"`——文案误导用户。

> 建议：将 ToolNotification 渲染从 Delete 专用改为通用——notification 消息文本由 handler 完整提供，不拼接外部前缀。已修复。

状态：✅ 已修复（Graph.vue 模板改为通用 `{{ activeNotification.message }}`，delete handler 自行拼接 `再次点击将删除："..."` 前缀）

### 信息 O-1：`style('border-color', '')` 重置方式隐式依赖 Cytoscape 默认回落

位置：`frontend/src/feature-tools/toolbar/move_node.ts` — 3 处（cancelPick、mousemove handler、deactivate）

现象：红色高亮清除使用 `cyNode.style('border-color', '')`，回落到 stylesheet 的 `#3d3d3d`。若未来有其他功能设置 per-element border-color（如 search-focus overlay），空字符串回退会错误清除。当前安全——仅 move handler 设置此样式。

> 建议：后续统一视觉预览样式时替换为 CSS class 或显式设回 `#3d3d3d`。

状态：⏳ 待确认

### 信息 O-2：collision notification 的 onCancel label 不够明确

位置：`frontend/src/feature-tools/toolbar/move_node.ts` — notification.onCancel

现象：碰撞 notification 的 `onCancel` 调用 `cancelPick` 回到待拾取，但 label 未与放置重试区分。用户可能误以为"取消"会退出整个 move 工具。

> 建议：UX 打磨——将 onCancel 的 label/行为与点击其他按钮的 deactivate 区分开。低优先级。

状态：⏳ 待确认

---

### 信息 O-3：move 工具右键应取消拾取而非退出工具

位置：`frontend/src/feature-tools/toolbar/move_node.ts` — `deactivate()` → `cancelPick()`

现象：当前右键经 mediator 统一 `deactivate → default`，move 工具会完全退出。但 move 是多段式操作（拾取→移动→放置），用户选错节点后应能通过右键取消当前拾取、回到待拾取状态继续操作，而非退出工具后再点按钮重新激活。

> 建议：move handler 区分"取消拾取"（回到待拾取）和"退出工具"（deactivate），与 delete/add-node 等单段操作的右键行为不同。实现方式待后续确定。暂不实施。

状态：⏳ 待确认
