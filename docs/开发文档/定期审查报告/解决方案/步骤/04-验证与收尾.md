# 04-验证与收尾

---

## 1. 全量测试 ⏳

### 目标

全量测试通过，确认步骤 02、03 改造无回归。

### 1.1. 全量测试

**子目标**：
  * `pnpm --filter frontend test` 全绿
  * `pnpm --filter @my-project/graph-engine test` 全绿（步骤 01 已过，回归确认）

难度：
* 不确定度：低
* 算法复杂度：无
* 工作量：小

---

## 2. 旧数据兼容验证 ⏳

### 目标

localStorage 旧数据（含 form / abstractionLevel 字段）读取正常。

### 2.1. 旧数据兼容

**子目标**：
  * localStorage 旧数据读取正常——派生函数忽略旧字段，从 childGraphId 推导
  * 手动冒烟：加载旧数据 → 渲染正常 → 抽象节点显示正确多边形

难度：
* 不确定度：中（旧数据字段被忽略的兼容性需实测确认）
* 算法复杂度：无
* 工作量：小

---

## 3. 框架评估 ⏳

### 目标

评估"持久化数据与派生值分离"框架 + graph_store 单例化改造的合理性，决定后续方向。

### 3.1. 框架评估

**子目标**：
  * 评估"持久化数据与派生值分离"框架的合理性（类型契约 + 派生函数 + shallowRef 持有）
  * 评估 graph_store 单例化改造（去死 ref + 移除 Pinia + interface+closure）的效果
  * 决定是否变更 CLAUDE.md 的广义 GraphData 定义（"状态层 + 派生层"两段式）
  * 决定是否跟进 degree 派生化（当前为增量维护）
  * 决定图级信号迁移方案的实施（步骤 02 子步骤 4 的评估结论）

难度：
* 不确定度：高（框架评估结论依赖前序阶段实际效果）
* 算法复杂度：无
* 工作量：小

### 决策项

**待确定决策**：

> **Q1**：CLAUDE.md 定义变更的时机？
> **建议**：步骤 04 验证通过后，向用户报告框架评估结论，由用户决定是否变更 CLAUDE.md。
> **理由**：文档修改必须由用户明确许可（CLAUDE.md 属文档范畴）。

**已确定决策**：
* 步骤 04 的完成标志 = 前端测试全绿 + 旧数据兼容 + 框架评估报告提交用户。