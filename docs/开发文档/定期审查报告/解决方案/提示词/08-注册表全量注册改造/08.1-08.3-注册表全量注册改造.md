# 08.1-08.3 注册表全量注册改造 — 提示词

## 当前状态

- `useLifecycle.ts` 的 `restoreLastRootTree` 启动时只加载 `lastActiveRootId` 的根图树（根图 + 子图），按 `isInRootTree` 过滤——注册表 = 已访问图的集合
- `graph_store.ts` 的 `loadGraphToView` 导航时 `registerGraph` 累积注册
- 跨图操作（`makeLookup`）只能查到已加载的图，未加载图操作失败（`BATCH_GRAPH_NOT_FOUND`）

## 目标

- 启动时全量加载所有持久化图进注册表（`registerAllGraphs`），注册表覆盖全部图
- `loadGraphToView` 移除 `registerGraph`（图已在注册表，仅切换视图）
- `lastActiveRootId` 语义收窄为"恢复上次视图"（初始 `graphViewId`），不再决定注册表内容

## 架构变更

```
旧：restoreLastRootTree（只加载当前根图树，isInRootTree 过滤）
  → loadGraphToView（导航时 registerGraph 累积）
  → 注册表 = 已访问图的集合

新：registerAllGraphs（全量加载全部持久化图）
  → 注册表 = 全部图
  → loadGraphToView 仅更新 graphViewId（无注册调用）
```

## 具体子任务

### 1.1. registerAllGraphs 只做全量注册

- `useLifecycle.ts`：`restoreLastRootTree` 改造为 `registerAllGraphs`——遍历 `listSavedGraphIds` 逐图 `loadGraph` 注册进注册表（corrupted 入开发者通道 `reportCorruptedGraph`，missing 跳过），**不再按 `isInRootTree` 过滤**（全部图都注册）
- **返回 void**（只做注册，不返回视图 ID）

### 1.2. 职责拆分：restoreLastActiveRootId + ensureWorkspaceRoot + Graph.vue 时序编排

- 原"返回上次视图根图 ID"逻辑（`loadLastActiveRootId` + 验证 kind 非 root / 不在注册表 + 清理）抽成独立函数 **`restoreLastActiveRootId(): GraphId | null`**（恢复上次视图根图 ID）
- `ensureWorkspaceRoot` **不再做注册**（无副作用）：调用 `restoreLastActiveRootId` 恢复上次视图根图，无则**写死创建兜底根图**（无外部参数，现有兜底逻辑不变）
- `Graph.vue` 负责**时序性编排**：`registerAllGraphs()` → `ensureWorkspaceRoot()` → `loadGraphToView(rootId)`

### 2.1. loadGraphToView 移除 registerGraph

- `graph_store.ts`：`loadGraphToView` 移除 `registerGraph(store.graphRegistry, loadedResult.graph)`——全量注册后图已在注册表，仅更新 `graphViewId` 切换视图
- 保留串行五步结构（加载 → 验证 → 换显示 → 根图切换判断 → 更新运行时状态），仅去掉注册步骤

### 3.1. 测试适配

- `useLifecycle.test.ts`：`restoreLastRootTree` 用例适配为 `registerAllGraphs`（只注册）+ `restoreLastActiveRootId`（恢复视图 ID）+ `ensureWorkspaceRoot`（不注册，写死保底）语义
- `useNavigation.test.ts` / `graph_store.test.ts`：loadGraphToView 移除注册后的行为适配（如测试依赖 loadGraphToView 注册图）
- `integration.test.ts` 及依赖 loadGraphToView 注册的测试：setup 中补 `registerAllGraphs()` 预注册（按真实启动路径适配）
- 全量测试绿（前端 191 + GE 175）+ 前端/GE type-check 无错误

### 3.2. 术语表更新

- `docs/开发文档/开发术语表.md`：`restoreLastRootTree` 条目更新为 `registerAllGraphs`（全量注册全部图）
- 术语表时间戳由 pre-commit 钩子自动同步

## 新增/修改文件

| 文件 | 职责 | 操作 |
|------|------|------|
| `frontend/src/graph/use-case/useLifecycle.ts` | restoreLastRootTree → registerAllGraphs（只注册）+ 新增 restoreLastActiveRootId + ensureWorkspaceRoot 不注册 | 修改 |
| `frontend/src/graph/graph_store.ts` | loadGraphToView 移除 registerGraph | 修改 |
| `frontend/src/views/Graph.vue` | 时序编排：registerAllGraphs → ensureWorkspaceRoot → loadGraphToView | 修改 |
| `frontend/src/graph/use-case/useLifecycle.test.ts` | 测试适配 registerAllGraphs / restoreLastActiveRootId / ensureWorkspaceRoot 语义 | 修改 |
| `frontend/src/graph/use-case/useNavigation.test.ts` | 测试适配（如依赖 loadGraphToView 注册） | 修改（如需要） |
| `frontend/src/graph/graph_store.test.ts` | 测试适配（如依赖 loadGraphToView 注册） | 修改（如需要） |
| `frontend/tests/integration.test.ts` | setup 补 registerAllGraphs 预注册 | 修改 |
| 依赖 loadGraphToView 注册的 feature-tools 测试 | setup 补 registerAllGraphs 预注册 | 修改（如需要） |
| `frontend/src/graph/graph_persistence.ts` | 注释中 restoreLastRootTree 引用更新 | 修改 |
| `frontend/src/graph/utils/graph_tree.ts` | 注释中 restoreLastRootTree 引用更新 | 修改 |
| `docs/开发文档/开发术语表.md` | restoreLastRootTree 条目更新为 registerAllGraphs | 修改 |

## 变更边界

**禁止修改**：

- `packages/graph-engine/` 全部
- `commitBatchToGraphs` / `undo` / `redo` 的行为语义
- `graphRegistry` 的引用替换机制（06.3 已定）
- localStorage 容量处理（本步骤不管，MVP 阶段）
- `loadGraphToView` 的串行五步结构（仅允许移除注册步骤）

## 验收标准

- [ ] `registerAllGraphs` 只做全量注册（返回 void），不再按根图树过滤
- [ ] 原"返回上次视图根图 ID"逻辑已抽成独立函数 `restoreLastActiveRootId`
- [ ] `ensureWorkspaceRoot` 不再做注册（无副作用），写死保底逻辑（无外部参数）
- [ ] `Graph.vue` 负责时序编排：registerAllGraphs → ensureWorkspaceRoot → loadGraphToView
- [ ] `restoreLastRootTree` 已重命名为 `registerAllGraphs`，全库无残留引用（grep 确认）
- [ ] `loadGraphToView` 无 `registerGraph` 调用
- [ ] `lastActiveRootId` 语义收窄为恢复上次视图（初始 graphViewId）
- [ ] `pnpm --filter frontend test` 全绿
- [ ] `pnpm --filter @my-project/graph-engine test` 全绿
- [ ] `pnpm --filter frontend type-check` 无错误
- [ ] `pnpm --filter @my-project/graph-engine type-check` 无错误
- [ ] 术语表 `restoreLastRootTree` 条目已更新为 `registerAllGraphs`

## subagent task 返回要求

返回内容需包含：

1. 自检清单：逐项确认上述验收标准（打勾或说明未完成原因）
2. 修改文件清单（含每个文件改动摘要）
3. 测试结果（前端 + GE 测试输出摘要 + type-check 结果）
4. 不确定事项：执行中发现任何不确定的决策（如 ensureWorkspaceRoot 适配、lastActiveRootId 验证逻辑、测试适配），按"不确定决策标记规则"报告，不得隐藏

## 注释要求

本任务涉及编写或修改代码注释（含 TSDoc）。动手前必须先阅读：`FOR-AGENTS/注释资料.md`。注释风格遵循该资料规范。