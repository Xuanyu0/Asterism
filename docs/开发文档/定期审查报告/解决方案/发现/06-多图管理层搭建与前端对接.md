# 06-多图管理层搭建与前端对接

---

## 发现者:@fixer（06.3 前端对接执行）

### 不确定事项

#### U-1：图级逆元过滤（日志组装）

* 状态：⏳ 待确认
* 位置：`frontend/src/graph/graph_store.ts`（日志 entry 组装）
* 问题：`applyBatches` 会为图级操作构造逆元（`createGraphReversal`，add ↔ delete 互逆）并放入 `result.reversalOperations`。契约要求"日志组装用 result.reversalOperations"但同时又要求"行为等价 + 禁止修改 applyEntry 行为"。若把图级逆元原样写入 entry，undo 三段式阶段二会把 add_graph 逆元当图内批执行——用空壳骨架覆盖阶段一从持久化恢复的完整图（数据丢失）。
* 处理：组装 entry 时**过滤掉图级逆元项**（保持旧日志格式：图级操作不产生 reversalOperations，由 graphSignals + 三段式兑现）；图级逆元留待 07 统一日志模型启用。
> 建议：07 启用图级逆元时，需同步改造 applyEntry 三段式（图级逆元作为普通操作执行）。

#### U-2：applyEntry forward 跨 item add_graph 兜底（潜在缺陷修复）

* 状态：✅ 已处理（行为保持性修复）
* 位置：`frontend/src/graph/graph_store.ts`（applyEntry forward）
* 问题：旧 forward 只在当前 item 内找 add_graph 兜底；新批模型下 add_graph 批与填充批在 entry.operation 中分离，填充 item 无法解析被注销的图 → redo 会跳过填充（旧代码对真实 deconstruct/induce 流程存在同样的潜在缺陷，被旧单 item 测试掩盖）。
* 处理：兜底查找扩展到 entry 全部 item。严格说 applyEntry 代码有改动，但行为未变（旧测试断言结果不变）。

#### U-3：delete_graph 测试数据适配（引擎空图语义）

* 状态：✅ 已处理
* 位置：`frontend/src/graph/undo_redo.test.ts`
* 问题：引擎 06.1/06.2 语义为 add_graph 只建空图、delete_graph 只删空图。旧测试"删除含节点的子图"在新语义下必然校验失败（DELETE_GRAPH_NOT_EMPTY）。
* 处理：改为空子图验证软删（注销 + 持久化保留 + undo 恢复注册），软删语义本身不变。

#### U-4：class_mapper.ts 修复（超出文件清单）

* 状态：✅ 已处理（06.1/06.2 遗留技术债）
* 位置：`frontend/src/cytoscape/mapper-utils/class_mapper.ts`
* 问题：`vue-tsc` 报 `Property 'form' does not exist on type 'KnowledgeNodeData'`——06.1/06.2 类型改造（form 改为派生值）遗留的前端错误，非 06.3 引入。
* 处理：最小修复（`node.form` → `deriveNodeForm(node)`），满足验收标准"vue-tsc 无错误"。
> 建议：确认该修复归属（本应属 03 步骤/派生值分离范围）。

#### U-5：useGraphOperation.ts 拆分（超出文件清单）

* 状态：✅ 已处理
* 位置：`frontend/src/graph/use-case/useGraphOperation.ts`（commitToCurrentGraph）
* 问题：该文件不在 06.3 契约文件清单内，但它调用 `commitBatchToGraphs`（旧形态），必须适配。
* 处理：`commitToCurrentGraph` 保留 `GraphOperation[]` 签名（move_node 等调用方传 compose 返回的 `GraphOperation[]`），内部按图级/图内分拆为独立批（`OperationBatch[]`）。

---

## 发现者:@orchestrator（06.2 多轮修正总结）

### 设计决策记录（供 oracle 评价参考）

本次 06.2 经历了多轮基于代码审视的设计深化，核心决策：

- **批次判别联合**：`OperationBatch = inGraph | graphLevel`——图级操作独立成批，applyBatches 按 kind if-else 分派，分类预处理消失
- **路由函数 / 路由逆元构造函数**：`executeGraphOperation`（纯函数 `(registry, op) → registry`）/ `createGraphReversal`（add ↔ delete 互逆，签名统一 `{ type, graph }`）
- **图级操作空图对称约定**：add_graph 只建空图、delete_graph 只能删空图（validateGraphOperation 强制）；DeleteGraphOperation 携带图数据（签名统一）
- **顺序正确性上移**：executeGraphOperation 不再查 latestGraphs（批内结果图），add_graph 批在填充批之前的顺序由 compose 保证
- **批次操作对象存在性校验**：inGraph 批的图必须存在（BATCH_GRAPH_NOT_FOUND），防止隐式创建幽灵图
- **validate 对称**：`validate_operation_in_graph.ts`（图内操作校验）/ `validate_graph_operation.ts`（图级操作校验）
- **两个 apply 顶层函数定义**：`apply_batch`（单图批 = 校验 + 分派 executeOperation）/ `apply_batches`（多批次 = 遍历批 + 校验 + 分派）

### 遗留待决

- **globalRules 命名**：`global_rules.ts` 是否改名（batchRule / graphRule / 保留 globalRule）待用户决定

---

## 发现者:@oracle（06.1-06.3 验证）

### 契约校对结论

**06.1 ✅ 全部通过 / 06.2 ✅ 通过（2 处文字级偏差，建议修文档）/ 06.3 ✅ 通过（3 处边界外必要适配）**

- 06.2 文字偏差：契约"graphSignals 累积由 applyBatches 调用方推导"（实现为内部累积，封装更好）；契约 2.6 批次示例顺序与"add_graph 批在填充批之前"规则自相矛盾（实现选规则，正确）
- 06.3 边界外适配：U-2（applyEntry forward 跨 item 兜底）、U-4（class_mapper 修复）、U-5（useGraphOperation 拆分）——均为"不改则测试挂"的必要适配

### 按重构意图评价（9 条设计意图）

- 多图管理层 GE 端落地 ✅ / 批次判别联合消除分类预处理 ✅ / 空图对称约定 ✅（最有价值决策）/ 路由函数四端对称 ✅ / 顺序正确性上移 ✅ / 存在性校验 ✅ / validate 对称 ✅ / 两个 apply 顶层函数 ✅ / 07 铺路 ✅（graphSignals 中间态 + 逆元序列完整）
- 前端日志/undo 仍是三段式 + graphSignals + 图级逆元过滤（U-1）——**06 设计内的中间态，07 消除**（刻意延迟，非缺陷）

### 关键发现（高优先级，预存缺陷）

#### A-1：正向 compose 批在 validate-all-first 下实际不可执行

* 状态：⏳ 回规划待用户裁决
* 位置：`induce.ts`、`diverge.ts`（Case B）经 `applyBatches` 完整执行
* 问题：子图填充批/当前图批内 `add_edge` 端点依赖批内 `add_node`（沟通节点/启发节点），applyBatch Phase 1 基于输入图校验 → `EDGE_SOURCE/TARGET_NOT_FOUND` → **整批丢弃**。**非 06 回归**（与 HEAD 版 pipeline/validate 逐行相同），但 06.3 验收测试从未用真实 compose 输出跑通管道（只模拟了 add_graph + add_node 填充）。目前 **dormant**：UI 侧 induce 是空数组 stub、diverge 无调用方，唯一真实接线的 deconstruct（子图批只有 add_node）恰好不触发。
* 影响：图内/图级平级管道对 4 个认知操作只有 2 个能真正跑通（deconstruct ✅ / internalize ✅ / induce ✗ / diverge Case B ✗）。
* 方向（待用户裁决）：链式校验（每 op 对运行中图校验）/ compose 前验证 + 正向 skipValidate / 拆批（边在端点批后独立成批）。修复时机：07 前修 / 并入 07 / 维持 dormant。

#### A-2：validateGraphOperation 防御缺口（中）

* 位置：`validate_graph_operation.ts`
* 问题：只做空图校验（契约如此），但 `add_graph` 对已存在 ID 静默覆盖（可覆盖非空图→数据丢失）、`delete_graph` 对缺失目标静默成功（no-op）——与 BATCH_GRAPH_NOT_FOUND 的"保险"哲学不一致。
> 建议：补 `GRAPH_ALREADY_EXISTS` / `GRAPH_NOT_FOUND`（07 前小改）。

#### A-3：低风险清理项

* `ComposeResult<Draft>` 仍带 `operations: GraphOperation[]`（未统一共享契约类型）
* `deconstruct.ts` 头部注释过期（cleaner S3 遗漏）：规则 2/3、usage 示例与实现不符

### 不确定事项裁决

| 项 | 裁决 |
|---|---|
| U-1 图级逆元过滤 | ✅ 保留（正确必要；07 启用时同步改 applyEntry 三段式）|
| U-2 applyEntry forward 兜底 | ✅ 保留（标注契约边界例外，新日志布局必要适配，07 自然消亡）|
| U-3 delete_graph 测试适配 | ✅ 保留（引擎空图语义必然，GE 端单测已覆盖非空拒绝）|
| U-4 class_mapper 修复 | ✅ 保留（归属应为 form 派生值改造遗留，建议补归属确认）|
| U-5 useGraphOperation 拆分 | ✅ 保留（调用方必改，S1 守卫实现干净）|
| S1 守卫抽取 | ✅ 保留（去重合理，补集正确）|
| S2 删图级兜底分支 + validateAddGraph | ✅ 删除正确（空图校验已由 validate_graph_operation 完整接管）|

### 质量与效果评价

- **重构质量高**：判别联合 + 单循环分派 + 路由函数对称 + 空图互逆约定，四端平级在 GE 端完整成立；前端 graph_store 成功瘦身为纯状态持有者。测试到位（GE 173 / 前端 191 / vue-tsc 实测全绿）。
- **主要风险**：A-1 预存缺陷（回规划）；A-2 防御缺口；全部改动未提交（38 files +884/-2074）；A-3 低风险清理。
- **07 前置条件基本就绪**：逆元序列完整产出 + item 逆序正确；07 提示词需注意"跳过 added 图逆元"语义是否保留 + 软删下 delete_graph 逆元空骨架的持久化细节。
