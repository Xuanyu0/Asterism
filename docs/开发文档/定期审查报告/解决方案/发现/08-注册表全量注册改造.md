# 08-注册表全量注册改造

---

## 发现者:@fixer（08 执行）

### 设计决策

#### D-1：registerAllGraphs 返回值校验实现

* 状态：✅ 已裁决（oracle：正确，通过）
* 位置：`frontend/src/graph/use-case/useLifecycle.ts`（registerAllGraphs）
* 问题：返回上次视图根图 ID 时用 `registry.get(lastRootId)`（因循环已全量注册）而非旧代码的显式 `loadGraph` 验证。
* 处理：corrupted 报告在循环中完成，missing 静默；不在注册表即视为 corrupted/missing → 清理 + null。行为与契约一致。
* oracle 裁决：与旧代码等价（listSavedGraphIds 派生自 localStorage key 扫描，不存在漏网情形）；四条路径（healthy / kind 非 root / corrupted / missing / 无历史）与契约逐一对上。
* 风格提示（低）：`registry.get()` 未走 `lookupGraph` 包装，与模块"统一使用包装函数"约定略有出入。

### 观察项

* **测试适配范围超出契约清单**：`loadGraphToView` 移除注册后，`integration.test.ts` + 6 个 feature-tools/mediator/useGraphOperation 测试因断言 graphView 依赖注册行为，必须补 `registerAllGraphs()` 预注册（按真实启动路径适配）。oracle 裁决：必要且正确，语义对齐而非漂移。
* **undo_redo.test.ts 无需改动**：其 loadGraphToView 调用未断言被加载图已注册（子图经 add_graph 信号注册、根图经 findParentGraph 惰性加载）。oracle 复核通过。
* **术语表 prettier 全表重排已回退**：对术语表跑 prettier 会重排整张表（188 行 diff），已回退并仅保留 1 行条目改动。

---

## 发现者:@oracle（08 校验）

### 契约校对结论

**08 ✅ 通过**（9 条验收标准 + 5 条变更边界全部满足）。执行者未越界、未漏项。无需回执行。

### 关键发现

#### A-1【中】useNavigation.ts:89 注释失实

* 位置：`frontend/src/graph/use-case/useNavigation.ts:89`
* 问题：「registry 只持有当前根图树，无法覆盖全部根图」——全量注册后已不成立（结论"listRootGraphInfos 扫持久化"仍有效，但理由错了）。
* 建议：更新注释（全量注册后 registry 覆盖全部图，listRootGraphInfos 扫持久化是为避免反序列化开销）。

#### A-2【低】useLifecycle.ts:2 模块头描述过时

* 位置：`frontend/src/graph/use-case/useLifecycle.ts:2`
* 问题：「工作根图树的恢复与兜底创建」描述过时，应为全量注册语义。

#### A-3【低】graph_persistence.ts:198 措辞

* 位置：`frontend/src/graph/graph_persistence.ts:198`
* 问题：「调用方需自行 loadGraph 并验证 kind === 'root'」措辞与实际实现（循环内已加载、经 registry 查证）略有出入。

#### A-4【低】前端测试数 192 vs 契约基准 191

* 新增 1 个测试（graph_store.test.ts 锁定"loadGraphToView 不再注册图"）属合理行为锁定，建议在步骤文档中记录。

### 不确定项裁决

| 项 | 裁决 |
|---|---|
| U-1 CLAUDE.md:156 仍引用 restoreLastRootTree | 应更新（活文档已失实），但需用户授权（文档修改规则 + 不在契约清单内）——回规划 |
| U-2 历史档案（定期审查报告/ 步骤/提示词/发现）引用保留 | 正确（历史不改写），用户确认豁免即可 |
| U-3 "loadGraphToView 成功但 graphView 为 null"陷阱态 | 契约允许（仅去注册步骤），已被测试固化；记入发现文档，后续步骤决定是否加防御（如未注册时返回 false） |

### 遗留待决

* **CLAUDE.md:156**：`restoreLastRootTree` 引用需更新为 `registerAllGraphs`（活文档失实）——待用户授权
* **A-1/A-2/A-3 注释问题**：useNavigation.ts:89 / useLifecycle.ts:2 / graph_persistence.ts:198——待用户决定是否修复