# 01-派生值分离与类型框架

---

## 发现者:@fixer

### 需要用户确定项

#### U-1：消费盘点遗漏（测试侧）

* 状态：⏳ 待确认
* 位置：`packages/graph-engine/tests/`（collision.test.ts / search.test.ts / pipeline.test.ts / deconstruct.test.ts / test_case_factory.ts）
* 问题：契约文件清单外，tests/ 下 5 个测试文件与 test_case_factory.ts 也在读写 form / abstractionLevel 字段。不改则测试不绿，已同步迁移（删除字面量、`form: 'abstract'` 构造改 `childGraphId`、deconstruct 断言改 childGraphId）。
* 影响：契约的"消费盘点"未覆盖测试侧；迁移已执行，需确认迁移方式符合预期。
> 建议：接受迁移结果（测试侧字段读写属同一改造范畴，不迁移则测试不绿）。
> 理由：测试数据构造与断言必须与类型定义同步，属紧耦合改动。

#### U-2：新测试位置偏差

* 状态：⏳ 待确认
* 位置：`packages/graph-engine/tests/core/derive.test.ts`
* 问题：契约写 `src/core/derive.test.ts`，但 vitest include 为 `tests/**/*.test.ts` 且现有全部测试在 `tests/`，故放 `tests/core/derive.test.ts`。
* 影响：测试文件位置与契约描述不一致（功能等价）。
> 建议：接受实际位置（tests/ 是 vitest 约定目录）。
> 理由：与现有测试组织一致，vitest 才能发现。

#### U-3：环防御实现粒度

* 状态：✅ 已确认（用户检查后改为抛错）
* 位置：`packages/graph-engine/src/core/derive.ts`
* 问题：原实现采用"沿推导路径 visited"返回 1 的静默防御。用户检查后确认：环是数据损坏，应直接抛错（fail fast），而非静默返回有限值。
* 现状：已改为环检测抛错（`throw new Error`），嵌套函数重构为顶层私有函数 `deriveLevel`（onPath / lookupGraph 显式传参，符合"私有函数放文件末尾"规范）。测试同步更新：环测试断言抛错，新增 DAG 共享子图用例（同一子图被多节点引用不误判环）。

#### U-4：引用节点语义

* 状态：⏳ 待确认
* 位置：`packages/graph-engine/src/core/derive.ts`
* 问题：`deriveAbstractionLevel` 接受 `NodeBase`，引用节点携带 childGraphId 时推导与知识节点一致（已补单测）。未发现语义差异。
* 影响：无（语义一致）。
> 建议：接受现状。
> 理由：引用节点复制 childGraphId（denormalization），沿链推导逻辑一致。

#### U-5：工作区既有改动

* 状态：⏳ 待确认
* 位置：`README.md`
* 问题：工作区存在预先未提交的 README.md 改动（删除"体验提示"段落），非本任务产出，未触碰。
* 影响：提交时需区分（README 改动与本次改造无关）。
> 建议：提交时排除 README.md 或单独提交。
> 理由：避免无关改动混入本次改造提交。

#### U-6：前端 28 处引用

* 状态：⏳ 待确认
* 位置：`frontend/`（class_mapper、bootstrap、preview_engine、test_case_factory 等）
* 问题：前端 28 处 form / abstractionLevel 引用属步骤 02 范围，未修改；步骤 02 执行时需同步迁移，否则前端类型检查将报错。
* 影响：步骤 01 完成后前端类型检查会失败（预期内，步骤 02 修复）。
> 建议：步骤 02 执行时同步迁移前端引用。
> 理由：契约明确前端属步骤 02 范围。

---

## 发现者:@orchestrator

### 可改进项

#### I-1：契约文件清单未覆盖测试侧

* 状态：⏳ 待确认
* 位置：`提示词/01-派生值分离与类型框架/01.1-派生值分离与类型框架.md`
* 现象：契约文件清单只列了 src/ 下文件，未列 tests/ 下读写字段的测试文件。
> 建议：后续提示词的文件清单应包含测试侧（grep 全包盘点后再定清单）。
> 理由：测试数据构造与断言与类型定义紧耦合，遗漏会导致执行期越界。

---

## 发现者:@cleaner

### 可改进项

#### I-2：internalize.ts 冗余条件判断（3 处）

* 状态：⏳ 待确认
* 位置：`packages/graph-engine/src/compose/cognitive/internalize.ts`（findNodeInGraphOrChildGraphs 内 3 处）
* 现象：`deriveNodeForm(kn.node) === 'abstract' && kn.node.childGraphId`——派生模型下两者定义等价，`&& childGraphId` 的原始防御语义（防 form 与 childGraphId 不一致的脏数据）已失去意义，只余 TS 窄化作用。读者会疑惑"abstract 已蕴含 childGraphId，为何再判一次"。
* 影响：可读性（低严重度）。
> 建议：重构为 `const childGraphId = kn.node.childGraphId; if (kn.node.role === 'knowledge' && childGraphId !== undefined)`——更贴合"childGraphId 是权威源"的新模型，且天然完成窄化。
> 理由：派生模型下 form 检查与 childGraphId 检查等价，保留双判断是机械迁移的残留。

#### I-3：deriveAbstractionLevel 重算成本

* 状态：⏳ 待确认
* 位置：`packages/graph-engine/src/core/derive.ts`
* 现象：每次调用沿 childGraphId 子树完整重算。若步骤 02 前端渲染层对每个节点逐个派生，复杂度达 O(N·子树深度)。
* 影响：渲染性能（步骤 02 接入时需评估）。
> 建议：步骤 02 接入时对同图节点做一次性批量派生或缓存（reselect 模式）。
> 理由：设计契约本就是"读取时计算不持久化"，非缺陷；但渲染层逐节点派生需评估成本。

---

## 发现者:@oracle

### 审查结论

**通过**。契约 9 项验收标准全部达成；测试全绿（20 文件 / 159 测试）；无越界执行；架构方向符合"GraphData 只存事实、派生值读取时计算"。

### 发现项复核

| 项 | 结论 | 说明 |
|----|------|------|
| U-1 测试侧消费盘点遗漏 | ✅ 接受 | 迁移方式正确，属紧耦合联动 |
| U-2 测试位置偏差 | ✅ 接受 | vitest include = `tests/**/*.test.ts`，位置正确 |
| U-3 环防御粒度 | ✅ 接受（措辞需修正） | 路径级 visited 正确；但"环返回值取 1"表述不精确——实际环场景顶层返回环外深度 + 1（不固定），功能上满足"不无限递归" |
| U-4 引用节点语义 | ✅ 接受 | 语义一致，已有单测覆盖 |
| U-5 README.md | ✅ 接受 | 提交时排除或单独提交 |
| U-6 前端 28 处引用 | ✅ 接受 | 步骤 02 同步迁移 |
| I-1 契约清单未覆盖测试侧 | ✅ 接受 | 后续提示词 grep 全包盘点 |
| I-2 internalize.ts 冗余条件 | ⚠️ 需修改（低严重度，不阻断） | 建议采纳 `const childGraphId = kn.node.childGraphId; if (role === 'knowledge' && childGraphId !== undefined)` 重构；可随步骤 02 或独立小提交处理 |
| I-3 重算成本 | ✅ 接受 | 步骤 02 接入时评估批量派生/缓存 |

### 可改进项

#### I-4：环防御测试断言过弱

* 状态：✅ 已确认（随环改抛错一并解决）
* 位置：`packages/graph-engine/tests/core/derive.test.ts`
* 现象：原断言 `typeof level === 'number'` + `>= 1` 无法锁定环场景行为。
* 现状：环测试改为断言抛错（`toThrow(/成环/)`），行为明确。

#### I-5：DAG 共享子图场景无测试

* 状态：✅ 已确认（已补测试）
* 位置：`packages/graph-engine/tests/core/derive.test.ts`
* 现象：路径级 visited 的优势点（同一子图被多节点引用不误判环）未被测试覆盖。
* 现状：新增 DAG 共享子图用例（sub-shared 被 sub-a / sub-b 引用，断言 = 2），锁定路径级 visited 行为。

#### I-6：契约类型未约束实现（用户检查发现）

* 状态：✅ 已确认（已修复）
* 位置：`packages/graph-engine/src/core/derive.ts`
* 现象：实现函数（`deriveNodeForm` / `deriveAbstractionLevel`）是普通 function 声明，未引用 graph_data.ts 的契约类型 `DeriveNodeForm` / `DeriveAbstractionLevel`。契约 type 只是"文档化"了，没有"强制"实现符合契约——签名写错时 TS 不报错。
* 现状：已修复——实现改为声明为契约类型（`export const deriveNodeForm: DeriveNodeForm = ...`），import 契约类型，签名不兼容时编译报错。测试全绿（160 个），tsc 无错误。

#### I-7：GE 端 tests/ 从未被 tsc 类型检查（VS Code 报错根因）

* 状态：✅ 已确认（已修复）
* 位置：`packages/graph-engine/tsconfig.json` / `tests/`
* 现象：tsconfig exclude 了 tests/，tests/ 从未被 tsc 类型检查。VS Code 用默认配置处理测试文件 → 不认识全局 test/describe（TS 2593）；且 12 个存量类型错误一直隐藏。
* 现状：已修复——新增 `tests/vitest.d.ts`（vitest/globals 类型声明）+ `tsconfig.test.json`（include src + tests，显式 exclude 覆盖继承）；修复 12 个存量类型错误（NodeRadiusMap 导入路径 5 处、induce.test.ts registry→lookupGraph 4 处、pipeline.test.ts 无效操作类型、derive.test.ts makeGraph 类型、test_case_factory.ts NodeRole→NodeKind）。`tsc -p tsconfig.test.json` exit 0，160 测试全绿。

### 需要用户确定项

#### U-7：环场景返回值语义

* 状态：⏳ 待确认
* 问题：环场景顶层返回值 = 环外深度 + 1（不固定）。环属数据损坏（operation_controller 层会提前拦截），任何有限值均可接受。
* 影响：环场景返回值无稳定语义。
> 建议：接受现状（防御点返回 1 与链中断防御一致的语义已达成），仅修正 U-3 措辞。
> 理由：环是数据异常，任何有限值可接受。

#### U-8：README.md 提交策略

* 状态：⏳ 待确认
* 问题：工作区存在预先未提交的 README.md 改动（删除"体验提示"段落），非本任务产出。
* 影响：提交时需区分。
> 建议：提交本步骤时排除 README.md，或单独提交。
> 理由：避免无关改动混入本次改造提交。