# 06-UI层重构：浮空窗生命周期归属

## 目标

浮空窗状态从全局 Pinia store（`ui_store`）迁出，归属 default 工具的交互会话；关闭机制从"9 处手动调用"收敛为"1 条外部点击规则 + 少量明确关闭点"；为未来其他工具复用浮空窗/确认弹窗能力提供共享组合式函数基础设施（`feature-tools/composables/`）。

## 1. 新建 useFloatingWindow 组合式函数 ✅

* **子目标**：`feature-tools/composables/useFloatingWindow.ts`——模块级单例（与 mediator / renderer 同款手法），承载浮空窗状态（展示数据 / 打开 / 关闭），内置外部点击关闭规则（全局 `pointerdown` 监听 + 容器外判定，按下即判定防拖拽误触），暴露容器注册 API（`registerContainer(el)`），关闭时保留"清空校验结果"联动。
* 不确定度：低（模式成熟，参照 mediator 单例手法）
* 算法复杂度：低（事件监听 + 容器包含判定）
* 工作量：中（新文件 + 监听生命周期管理）

## 2. ToolHandler 接口扩展 + default_tool 接入 ✅

* **子目标**：`ToolHandler` 新增可选字段暴露浮空窗数据（响应式门面，转发单例状态）；`default_tool` 打开浮空窗时写入状态；**confirm 写入逻辑（构造 update 操作 + commitBatchToGraph）保留在 `default_tool.onConfirm`**，`ToolHandler.onConfirm` 契约不变。
* 不确定度：低
* 算法复杂度：低
* 工作量：低

## 3. GraphNodeWindow.vue 消费路径切换 ✅

* **子目标**：组件改经 default handler 门面读取浮空窗数据（不再直接读 ui_store）；挂载时注册自身根元素为容器；**保留叉号按钮**。
* 不确定度：低
* 算法复杂度：低
* 工作量：低

## 4. 清理旧关闭点 + ui_store 瘦身 ✅

* **子目标**：删除 4 处冗余关闭（`delete.ts` ×2、`GraphNavigationCard.vue` 切换图谱、`default_tool.ts` 双击内部）；保留 4 处明确关闭点（confirm 成功、叉号、`mediator.activate` 程序化切工具语义守护、`Graph.vue` 双击导航前——click 先开窗的时序特殊）；`ui_store` 移除 `floatingWindowData`，仅剩 `pendingCanvasFocusId`。
* 不确定度：中（删点后需确认无遗漏场景，外部点击规则对 Cytoscape 画布与 DOM 点击的覆盖）
* 算法复杂度：低
* 工作量：低

## 5. 测试与回归 ✅

* **子目标**：新增组合式函数测试（外部点击关闭规则、打开/关闭/容器注册）；检查现有测试对 `ui_store.floatingWindowData` 的引用并修正；跑前端全部测试确认无回归。
* 不确定度：中（现有测试引用面未知，需 grep 确认）
* 算法复杂度：低
* 工作量：中

### 影响范围（重构前后对比）

```
重构前                                      重构后
                                       
ui_store:                               ui_store:
  floatingWindowData  ← 全局状态          floatingWindowData ✗（移除，仅剩画布定位）
  pendingCanvasFocusId
                                        feature-tools/composables/
关闭点（9 处，义务扩散）：                    useFloatingWindow.ts（模块级单例）
  mediator.activate         ✗→保留(守护)      data / open / close
  delete.ts ×2              ✗→删除            registerContainer(el)
  GraphNavigationCard       ✗→删除            pointerdown 外部点击规则 → close
  Graph.vue 双击前          ✗→删除（行为收进 default_tool 双击内）
  default_tool 双击内       ✓→保留（工具自己管理）
  GraphNodeWindow 叉号      ✓→保留         关闭点（5 处，规则收敛）：
  default_tool confirm     ✓→保留           1. 外部点击规则（替代 4 处）
  default_tool.onConfirm   ✓→保留(写入)      2. confirm 成功
  ui_store.close 内联动     →迁移            3. 叉号按钮
                                            4. mediator.activate（语义守护）
                                            5. default_tool 双击导航内（click 先开窗，时序特殊）

消费路径：                                消费路径：
  GraphNodeWindow → ui_store              GraphNodeWindow → mediator.registry → default handler 门面
  default_tool → ui_store                 default_tool → useFloatingWindow() 单例
```

涉及文件：新增 `feature-tools/composables/useFloatingWindow.ts`；修改 `feature-tools/types.ts`、`feature-tools/default_tool.ts`、`components/GraphNodeWindow.vue`、`feature-tools/mediator.ts`、`feature-tools/toolbar/delete.ts`、`components/GraphNavigationCard.vue`、`views/Graph.vue`、`ui/ui_store.ts`；可能受影响：`feature-tools/mediator.test.ts` 等测试。

## 决策项

**待确定决策**

> **Q1**：外部点击规则是否同时覆盖"工具栏按钮点击"（DOM 点击在容器外，天然覆盖）与"快捷键切换工具"（无点击事件，不覆盖）？
>
> **建议**：覆盖前者（外部规则天然生效），后者因项目尚无键盘监听体系而搁置。理由：确认的决策范围只处理点击路径，快捷键留待未来统一设计。

**已确定决策**

- 浮空窗状态归属 default 工具的交互会话，不留在全局 store
- 非编辑点击（外部点击）自动关闭浮空窗，替代大部分手动关闭点
- `ToolHandler` 接口扩展暴露浮空窗数据，组件消费路径走接口门面（浮空窗数据修改是工具使用流程的一部分，不可割裂）
- confirm 写入逻辑保留在 `default_tool.onConfirm`（构造 update 操作 + commitBatchToGraph），`ToolHandler.onConfirm` 契约不变
- 保留叉号按钮（主动关闭路径，与外部点击规则共存）
- 键盘快捷键触发关闭——搁置（项目暂无键盘监听体系）
- 组合式函数位置：`feature-tools/composables/`（工具层共享组合式函数目录；与 `frontend/src/composables/` 的"通用无业务语义 UI 辅助"定位区分）
- 组合式函数采用模块级单例形态（浮空窗是全局唯一 UI 单元；与 mediator / renderer 同款手法）
- 外部点击判定使用 `pointerdown` 而非 `click`（按下即判定，防拖拽画布后的 click 误触发）
- 关闭时保留"清空校验结果"联动（原 `ui_store.closeFloatingWindow` 行为迁移至新组合式函数）
- 保留 `mediator.activate` 内的关闭（程序化切工具的语义守护，不依赖点击事件）
- 双击导航的关闭收进 `default_tool.onNodeDoubleClick` 内部（click 先于 dblclick 触发，双击时浮空窗已被 onNodeClick 打开，时序特殊）；`Graph.vue` 装配层不直接操作浮空窗状态，仅做激活工具检查与转发——双击逻辑由 default 工具经 `ToolHandler.onNodeDoubleClick` 接口接入

## 执行记录

- 执行契约：`提示词/06-UI层重构/6.1-浮空窗生命周期归属重构.md`
- 执行：@fixer（16/16 验收）→ @cleaner（简化）→ @oracle（15/15 验收 + 收口裁决）→ 收口修复（外部点击误清校验回归）
- 产出 commit：
  - `aa7b0b5` refactor: move floating window to default tool（代码）
  - `9c5a080` docs: add P3 planning for window refactor（规划文档）
- 测试：113/113 通过（frontend）
- 发现项：见 `发现/06-UI层重构.md`（2 项待确认）
