# 07-图数据适配层

---

## 发现者:@fixer（07.1 导航适配层）

### 说明：适配层 store 解析采用"工厂创建时捕获"

* 位置：`graph/adapters/useNavigationAdapter.ts`
* 现象：工厂创建单例时捕获 `useGraphStore()` 实例（非懒解析）。单测因此采用"整个 describe 共享一个 Pinia + beforeEach 复位"而非"每测试新建 Pinia"——否则模块级单例持有的旧 store 会在新 Pinia 下失效。生产语义一致（Pinia 启动时安装一次）。
* 状态：⏳ 待确认（与 07.2 的 lazy 解析不一致，见 07.2 发现 1）

### 说明：无图态测试用直置模拟

* 位置：`useNavigationAdapter.test.ts`
* 现象：store 无公开卸载入口，无图态用例直接置空 `store.graphView` / `store.graphPath` 模拟（已加注释）。后续若 store 提供卸载 API 可替换为真实路径。

### 说明：RootGraphInfo 类型再导出

* 位置：`useNavigationAdapter.ts`
* 现象：为使 NavigationPanel 完全不依赖 graph_store 模块，适配层新增 `export type { RootGraphInfo }` 一行再导出（契约未明示的最小补充）。若希望 NavigationPanel 直接 type-import 自 graph_store 可回退。

### 说明：GraphNavigationCard 视图模型类型取舍

* 位置：`GraphNavigationCard.vue`
* 现象：最终采用"直接消费适配层 `NavigationSegment` 类型"（经解构别名 `pathSegments` 传给 Dock，结构上 `NavigationSegment` ≡ `PathSegment`）。`PathSegment` 类型 import 已从该组件移除，但 `navigation-card/types.ts` 的 `PathSegment` 仍被 Dock.vue 使用未删（组件域内部共享，非本次范围）。

### 说明：NavigationPanel 技术债务已消除

* 现象：AGENTS.md 记录的"子组件直调 store 写方法"技术债务已随 07.1 迁移消除（NavigationPanel 改经适配层）。

---

## 发现者:@fixer（07.2 工具层适配）

### 说明：move_node.ts 确认无校验同步

* 现象：grep 验证其 `placeAttempt` 仅读取 `batchResult.validation.valid` 做 UI 决策、无 `lastValidationResult` 直写，按契约"仅换提交入口"处理。注：`commitBatchToGraph` 内部本就同步 `lastValidationResult`，故收敛前后全部 7 个工具行为严格等价——各工具的显式二次写入本就是冗余。

### 说明：适配层 store 解析采用 lazy 而非捕获

* 位置：`graph/adapters/useGraphOperationAdapter.ts`
* 现象：初版仿 07.1 在工厂创建时捕获 store，导致既有工具测试（delete/fold/move_node 等按测试重置 Pinia）操作落到旧 store、9 个用例失败。改用 useFloatingWindow 的 lazy 解析模式（方法调用时 `useGraphStore()`）后全绿。
* 状态：⏳ 待确认（**与 07.1 的捕获模式不一致**——07.1 的导航适配在按测试重置 Pinia 的测试场景下同样存在此隐患，是否统一为 lazy 待裁决）

### 说明：move_node.test.ts 测试名保留

* 现象："无碰撞放置后 commitBatchToGraph 被调用"测试名保留未改——行为断言仍成立（applyToCurrentGraph 内部链至 commitBatchToGraph），改名属纯美化且测试文件不在契约文件表内。

### 待办：operation_controller.ts 的 7 处直写残留

* 位置：`frontend/src/ui/operation_controller.ts`
* 现象：7 处 `lastValidationResult =` 直写。契约变更边界明确禁止触碰该文件（历史遗留，另行排期），故本次按"工具层无直写"达成；该文件的直写清理应在后续排期中经适配层统一。
* 状态：⏳ 待确认（列入 operation_controller 迁移排期）

### 不确定：setValidationFailure 命名

* 位置：`useGraphOperationAdapter.ts`
* 现象：预校验收口方法命名取对称于 store 的 `clearValidationResult`，语义聚焦"预校验失败收口"。若用户偏好其他命名（如 `recordValidationFailure`）可调整。
* 状态：⏳ 待确认

---

## 发现者:@cleaner（07 简化审查）

### 修复：useGraphOperationAdapter 冗余校验同步删除

* 位置：`graph/adapters/useGraphOperationAdapter.ts`
* 现象：`applyToCurrentGraph` 内 `graphStore.lastValidationResult = result.validation` 是冗余——`commitBatchToGraph` 内部（graph_store.ts L353）无条件同步。fixer 07.2 已认定工具层同类二次写入"本就是冗余"，适配层又把同一模式带回来。
* 处理：删除冗余写入并同步改写 JSDoc / 模块头契约 / 防御分支注释；单测 `store.lastValidationResult` 断言仍绿，行为可证等价。
* 状态：✅ 已确认

### 修复：NavigationPanel import 分组 + 排版

* 位置：`components/navigation-card/NavigationPanel.vue`
* 现象：`useNavigationAdapter` 值导入与 `type RootGraphInfo` 类型导入同组，违反 CLAUDE.md「type import 和普通 import 不要混在同一组」；defineProps 前双空行。
* 处理：拆分两组 + 合并双空行。
* 状态：✅ 已确认

### 修复：陈旧术语清理（3 处）

* 位置：`cognition/deconstruct.ts`、`feature-tools/default_tool.ts`、`toolbar/fold.ts`
* 现象：JSDoc 仍写「applyBatch」（已无此调用）、无插值模板字符串 `\`fold\``。
* 处理：更新为适配层术语 / 去反引号。
* 状态：✅ 已确认

### 裁决待定：两个适配层 store 解析不一致（capture vs lazy）

* 位置：两个 adapter 文件
* 现象：`useNavigationAdapter` 工厂创建时捕获 `useGraphStore()`；`useGraphOperationAdapter` 方法调用时 lazy 解析。既有规范参照 `useFloatingWindow`（close() 内 lazy）——lazy 才是代码库已确立模式。捕获模式还迫使 `useNavigationAdapter.test.ts` 采用「describe 共享一个 Pinia」的非标准结构。
* 建议：统一为 lazy（机械改动 + 测试可恢复为每用例独立 Pinia）。属 @oracle 裁决项，cleaner 未擅自改。
* 状态：⏳ 待 oracle 裁决

### 裁决待定：适配层方法命名与 store 词汇脱节

* 位置：`useNavigationAdapter.ts`
* 现象：`createRoot` / `deleteRootTree` / `listRootInfos` vs store 的 `createRootGraph` / `deleteRootGraphTree` / `listRootGraphInfos`——去掉领域后缀「Graph」，与全库 graph/Graph 词汇体系脱节（grep `deleteRootGraphTree` 无法再命中 NavigationPanel 调用点）。
* 状态：⏳ 待用户裁决（fixer 让适配层自洽导航词汇的取舍 vs CLAUDE.md 符号可搜）

### 说明：setValidationFailure 命名保留

* 状态：✅ 已确认（保留现名；备选 recordValidationFailure 无强依据）

### 说明：graph_store 私有化函数位置

* 现象：私有函数 `deleteAndUnregisterGraph` 位于文件中段，CLAUDE.md 偏好私有辅助放文件末尾；但红线限定该文件只允许修正表述瑕疵，未做移动。
* 状态：✅ 已确认（维持现状）

---

## 发现者:@oracle（07 最终验证）

### 验证结论

- 两份契约 21 条验收标准全部达成（07.1 的 11 条 + 07.2 的 10 条，含 2 项带说明）
- cleaner 的 6 处修复逐项核查无引入问题；核心前提（`commitBatchToGraph` 无条件同步 `lastValidationResult`，graph_store L353）已从源码实证
- 依赖方向红线守位：graph/ 域不反向依赖 components/
- 回归风险：低（130 用例全绿 + vue-tsc 干净 + 守卫/构造/UI 分支逐工具核对零变更）

### 裁决：两适配层 store 解析统一为 lazy（应修）

* 位置：`useNavigationAdapter.ts`（捕获）vs `useGraphOperationAdapter.ts`（lazy）
* 现象：代码库唯一既有单例参照 `useFloatingWindow` 是 lazy（close() 内 useGraphStore）；捕获模式在"单例创建早于 Pinia 安装 / 多 Pinia 场景（测试、热重载）"下持旧 store 静默失效。
* 裁决：统一为 lazy。改动：`createNavigationAdapter` 去掉顶层 useGraphStore，computed getter 与方法体内调用；`useNavigationAdapter.test.ts` 恢复"每用例独立 Pinia"标准结构（无图态用例改用真实"不加载图"路径）；`useGraphOperationAdapter.test.ts` 一并恢复标准结构；修正 operation 适配测试头注释"内部持有 Pinia store"陈旧表述。
* 状态：✅ 已确认（已执行：130/130 全绿；nav 测试采用 `vi.resetModules()` + 动态 import 重建单例——模块级单例的 computed 缓存首次求值依赖，跨 Pinia 重置不失效，vitest 将 vue/pinia 外部化无双实例问题）

### 裁决：适配层方法命名保留现名（不改回 Graph 后缀）

* 位置：`useNavigationAdapter.ts`（`createRoot` / `deleteRootTree` / `listRootInfos`）
* 现象：与 store 词汇（createRootGraph 等）脱节，但契约 07.1 L45-47 白纸黑字列出现名，执行无违约；适配层是领域门面，Graph 后缀在同域内是噪声。
* 裁决：保留现名。确立策略——**适配层透传方法 JSDoc 必须标注透传目标**（grep 两跳可达：`createRootGraph` → JSDoc → `createRoot` → 调用点），防止后续适配层（步骤 05 错误翻译落点）命名漂移。
* 状态：✅ 已确认

### 说明：applyToCurrentGraph 空图防御分支

* 现象：空图守卫返回 `{ valid: false, issues: [] }` 且不写校验状态，超出契约"不做额外兜底"字面。
* 判定：保留合理（调用方守卫使该路径不可达，此分支把潜在崩溃转安全失败），已文档化。

### 建议：AGENTS.md 技术债务描述更新

* 现象：债务形态已从"子组件直调 store"变为"子组件经适配层写 store"，AGENTS.md「已知技术债务」段落过时。
* 状态：✅ 已确认（用户已许可，已更新为"经适配层调用 + 中间路径约束"表述）

### 建议：move_node.test.ts 测试名 / snapthreshold 空白

* 现象：测试名"commitBatchToGraph 被调用"保留（行为断言仍成立）、`snapthreshold: 32` 双空格已修。
* 状态：✅ 已确认（无害）

---

## 发现者:@fixer（07.3 store 大幅瘦身）

### 说明：isInRootTree 保留在 store（契约假设偏差）

* 位置：`graph/graph_store.ts`
* 现象：契约假设 `isInRootTree` 仅服务 deleteRootGraphTree，但实际 `initRegistry`（保留函数，逻辑不可动）也使用它。处理：store 保留 isInRootTree 为私有函数（不入 return 对象），适配层 deleteRootGraphTree 内另有一份同逻辑副本（~17 行重复）。
* 状态：⏳ 待确认（若希望消除重复，可后续将 initRegistry 的树归属判断抽到共享模块，超出本次边界）

### 说明：AGENTS.md 短名表述过时

* 位置：`frontend/src/components/AGENTS.md`
* 现象：既有未提交改动描述适配层短名 createRoot/deleteRootTree，现与改名后的完整名不符。文档属边界禁止触碰。
* 状态：⏳ 待文档维护者同步（07 收尾时可一并处理）

### 说明：move_node.test.ts 测试名 / integration.test.ts 迁移

* 现象：move_node.test.ts 测试名"commitBatchToGraph 被调用"为纯描述性名称（断言行为结果），保留未改；integration.test.ts 不在契约文件表但被 4.5 覆盖，已迁移 4 处直调。
* 状态：✅ 已确认

---

## 发现者:@cleaner（07.3 简化审查）

### 修复：bootstrap.ts 缩进归位

* 位置：`frontend/src/dev/bootstrap.ts`
* 现象：`commitBatchToGraph → applyToCurrentGraph` 机械迁移遗留 6 个块约 35 处缩进错位（纯空白）。
* 处理：统一归位到 12/16 空格；`git diff --word-diff` 逐 token 验证内容零变更。
* 状态：✅ 已确认

### 确认：等价性 / 规范逐条核对

* 适配层迁入函数与原 store 逻辑等价、无冗余（逐行对照）；store 剩余 import 全部在用
* lazy 统一已落地（两适配层 + 两测试文件标准结构自洽）
* 短名改完整名彻底（唯一残留 AGENTS.md，边界外）
* operation_controller 临时过渡注释到位（实例化处 + 4 调用点）
* store 头注释职责边界表述准确（判定标准 grep 实证全部成立）
* isInRootTree 双副本双向交叉引用清晰，未抽共享模块（待确认项）
* 状态：✅ 已确认

### 说明：operation_controller 头部规则 3 陈旧表述

* 位置：`ui/operation_controller.ts`（文件头规则 3："所有图操作通过引擎 compose → graphStore.applyBatch 链路执行"）
* 现象：历史遗留陈旧表述（实际已不经 applyBatch 直调）。仅允许临时过渡改动范围内的微调，属 operation_controller 迁移排期时一并清理。
* 状态：⏳ 待确认（列入 operation_controller 迁移排期）

---

## 发现者:@oracle（07.3 最终验证）

### 验证结论

- 07.3 契约 11/11 达成（2 项带说明），回归风险低，可进入 07 收尾
- store 瘦身彻底（7 函数迁出、return 收窄）；保留函数零行为变更（diff 实证 body 零改动）；适配层迁移逐行等价；短名改完整名彻底；operation_controller 过渡保守；bootstrap 内容零漂移（`git diff -w` 独立验证）
- 136 用例全绿 + vue-tsc 干净

### 裁决：isInRootTree 双副本——接受（本次），后续抽共享模块

* 位置：`graph_store.ts`（私有，initRegistry 依赖）+ `useNavigationAdapter.ts`（deleteRootGraphTree 副本）
* 现象：契约假设 isInRootTree 仅服务 deleteRootGraphTree，但 `initRegistry`（保留函数，契约禁止动其逻辑）也依赖它（graph_store L179）。执行者保留 store 私有副本 + 适配层副本（~17 行），JSDoc 双向交叉引用。
* 裁决：当前边界内无法消除（initRegistry 不可改、graph/ 其他文件契约禁止改、新建共享模块超出文件表）；纯查询函数，重复风险低但真实存在。
* 后续建议：在 operation_controller 迁移排期（或 07 收尾独立小步）中抽取共享模块（候选：graph_persistence.ts 导出或新建 graph/ 工具文件），两处同源引用。
* 状态：✅ 已确认（07 收尾已消除：新建 `graph/utils/graph_tree.ts` 单份 `isInRootTree`，store 与适配层均经 utils import；node_radius / issue_mapper 同步迁入 `graph/utils/`）

### 建议：AGENTS.md 短名同步

* 现象：上次用户许可的更新用了 `createRoot`/`deleteRootTree`，07.3 改名后描述与实际不符（代码库已无此标识符）。
* 状态：⏳ 待用户许可（07 收尾一并处理，同步为 `createRootGraph` / `deleteRootGraphTree`）

### 建议：useNavigationAdapter.test.ts 的 vi.resetModules() 隐性契约

* 现象：依赖 vitest 将 vue/pinia 外部化——若未来取消外部化（双实例问题），此策略失效。
* 建议：测试头注释补"若外部化配置变更需重新评估"；或将单例重置收敛为适配层测试专用重置入口。
* 状态：⏳ 待确认（低严重度，可后续处理）
