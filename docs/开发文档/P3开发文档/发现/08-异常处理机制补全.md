# 08-异常处理机制补全

---

## 发现者：@fixer（08.1 执行期）

### 不确定：createRootGraph 幂等检查被迫追加 `.ok`

- 位置：`frontend/src/graph/graph_store.ts` 的 `createRootGraph`（`if (opts?.id && loadGraph(opts.id))` → 追加 `.ok`）
- 问题：契约变更边界明列"createRootGraph 禁止修改"，但 `loadGraph` 改判别联合后，`if (opts?.id && loadGraph(opts.id))` 对判别联合对象恒为 truthy——不追加 `.ok` 则幂等保证（"指定 ID 且图已存在则跳过创建"）静默失效，每次 `createRootGraph(title, { id })` 都不创建直接返回 ID，bootstrap 种子数据与 dev 路径损坏
- 影响：契约两条约束（"createRootGraph 禁止修改" vs "行为保持现状 / 消费点无遗漏"）直接冲突
- 状态：✅ 已确认（用户确认）
  > 建议：接受 `.ok` 追加（行为等价的必要最小改动，否则引入 bug）。
  > 理由：行为保持是契约最高优先级，判别联合化是签名变更的必然延伸。

### 信息：判别联合消费点超出契约清单 4 处

- 位置：`graph_tree.ts`（isInRootTree）、`dev/test_case_factory.ts`（银牌图存在性）、`useNavigationAdapter.ts`（deleteRootGraphTree 树成员收集）、`graph_persistence.ts`（localStorageAdapter.load 桥接）
- 现象：契约 2.2 调用方清单未列这 4 处，但 grep 验收"消费点无遗漏"兜底要求全部适配。其中 `localStorageAdapter.load` 因引擎 `PersistenceAdapter` 契约保持 `GraphData | null`，以桥接返回 null（判别联合 → null），附注释说明
- 状态：✅ 已确认（grep 验收的自然延伸，无行为变更）

---

## 发现者：@fixer（08.2 执行期）

### 不确定：链断裂双重 console.warn（重复噪声）✅ 已解决（@cleaner 修复）

- 位置：`frontend/src/graph/graph_store.ts` 的 `buildGraphPath`（父缺失 break 处）+ `loadGraphToView`（末端 `terminal.parentGraphId` 检测处）
- 现象：`buildGraphPath` 唯一调用方是 `loadGraphToView`，同一断链事件会触发两次内容相同的 `console.warn`——一次在 buildGraphPath 断裂点，一次在 loadGraphToView 末端完整性检测；环场景下还会在 `CYCLE_DETECTED` 之后误导性追加一条 `ANCESTOR_CHAIN_BROKEN`（断裂原因实为环而非父缺失）
- 处理：@cleaner 删除 `loadGraphToView` 末端重复报告块，检测与开发者通道记录收敛到 `buildGraphPath` 回溯处就地完成，每条异常恰好一条 warn；相关 JSDoc 同步修正
- 状态：✅ 已确认（修复后环场景不再错误归因，测试断言仍由 buildGraphPath 就地报告满足）

### 不确定：initRegistry 子图预加载循环的 corrupted 报告

- 位置：`frontend/src/graph/graph_store.ts` 的 `initRegistry`（子图预加载循环）
- 问题：契约 3.3 仅明确根图 corrupted 报告，未明确循环内子图；fixer 按"同属数据损坏场景"对循环内 corrupted 也补了报告（missing 保持静默）
- 状态：✅ 已确认（用户确认）
  > 建议：接受（子图损坏同样需要可见性，与根图一致）。

### 可改进：两处 TODO 注释最小修正

- 位置：`graph_store.ts` 文件头 + `initRegistry` JSDoc
- 事实：原 TODO 描述过时（"祖先链断裂仅写告警"、"写 lastValidationResult 或返回状态"），已按开发者通道语义修正为现状
- 状态：✅ 已确认（注释卫生，无行为影响）

---

## 发现者：@fixer（08.3 执行期）

### 信息：新建 graph_persistence.test.ts 集中断言判别联合分支

- 位置：`frontend/src/graph/graph_persistence.test.ts`
- 事实：项目此前无专属 persistence 测试文件（missing/ok 仅散落于 integration / useNavigationAdapter 测试），corrupted 分支完全无覆盖；按 4.1 语义新建集中断言三分支
- 状态：✅ 已确认

### 信息：sentinel 断言方式（强于 toBeNull）

- 位置：`frontend/src/graph/graph_store.test.ts`
- 事实：missing / corrupted 的"无状态写入"以预置 `{ valid: false, issues: [] }` sentinel + 断言不变实现，同时覆盖"不写"与"不清"
- 状态：✅ 已确认

### 可改进：initRegistry corrupted 未单测

- 位置：`frontend/src/graph/graph_store.ts` 的 `initRegistry`
- 事实：corrupted 报告共用 `reportCorruptedGraph`（输出格式已在 loadGraphToView 层断言验证），且需先构造 `lastActiveRootId` 持久化前置，4.2 未列 initRegistry 专用断言
- 状态：✅ 已确认（用户确认）
  > 建议：如需要可后续补 initRegistry 专用断言（构造持久化前置后断言 console.warn）。

---

## 发现者：@oracle（验证期）

### 可改进：corrupted 父图归因粒度丢失（低优先级）

- 位置：`graph_store.ts` 的 `buildGraphPath`（惰性加载父图处）
- 事实：`loadGraph(parentId)` 返回 corrupted 时未按 reason 细分，统一落入 `reportBrokenAncestorChain`（"祖先图谱 X 不可达"），不产出 `CORRUPTED_GRAPH`。信息完整度达标（parentId 在 message 中），且该辅助 JSDoc 已明示"缺失或损坏"语义——属有意的语义折叠
- 状态：✅ 已确认（用户确认）
  > 建议：如需精确归因，可在此处按 `result.reason` 分派两条报告（低优先级，非契约要求）。

### 可改进：测试覆盖缺口（非契约强制）

- 位置：`graph_store.test.ts`
- 事实：
  - `CYCLE_DETECTED` 无测试（环场景是 cleaner 归因修正的关键点，值得补防回归）
  - ANCESTOR 断言只查 code 字符串，未断言 message 中 graphId / terminalId / 缺失父图 id 三要素（验收要求信息完整）
  - `createRootGraph` 的 `.ok` 幂等路径无直接测试（它是 08.1 唯一越界但必要的改动，`dev/AGENTS.md` 承诺"内置幂等保护"）
- 状态：✅ 已确认（用户确认）
  > 建议：补 3 条防回归断言（环报告 / ANCESTOR 三要素 / createRootGraph 幂等不覆盖）。

### 行为边界：initRegistry 循环内 corrupted 报告先于树成员判定

- 位置：`graph_store.ts` 的 `initRegistry`（子图预加载循环）
- 事实：corrupted 报告发生在 `isInRootTree` 检查之前——不属于当前根图树的损坏图也会被报告。语义可辩护（localStorage 中确存在损坏数据，开发者通道可见性合理）
- 状态：✅ 已确认（用户确认）
  > 建议：接受此行为边界（开发者通道可见性合理），仅作记录。

### 不确定：CLAUDE.md 工作区排版改动 ✅ 已确认（非本次产出）

- 位置：`CLAUDE.md` L21（Runtime 层定义段，粗体标记内补一个空格）
- 问题：契约禁止修改任何文档，但工作区存在该改动
- 事实：已核实在 08 任务开始前即存在（`git diff --stat` 初始状态即 `M CLAUDE.md`），fixer 与 cleaner 均确认未触碰——非本次工作流产出（用户或其他会话的排版修正），无需回退
- 状态：✅ 已确认

---

## 发现者：@fixer（08.5 执行期）

### 信息：NodeData.label 为必填 string，未加 undefined 守卫

- 位置：`packages/graph-engine/src/types/graph_data.ts`（NodeBase.label）+ `core/validate.ts`
- 事实：`label` 必填 string（EdgeData 的 `label?: string` 属边非节点）；EMPTY_LABEL 判定用 `label.trim() === ''` 直白形式，未加 undefined 守卫（手写 JSON 缺字段经 `as` 断言流入时 trim 会抛 TypeError，属可选加固：`!label?.trim()`）
- 状态：⏳ 待确认
  > 建议：接受现状（类型系统保证必填），加固可后续可选处理。

### 信息：引擎测试用 `it()` + vitest import（与前端规范差异）

- 位置：`packages/graph-engine/vitest.config.ts`（未配 `globals: true`）+ 引擎测试文件
- 事实：项目测试规范（globals + `test()`）仅对 frontend 生效；引擎包 vitest 未启用 globals，`import { describe, it, expect }` + `it()` 是运行时必需。新增测试沿用引擎既有风格，未强行转换
- 状态：✅ 已确认（配置差异，非规范违反）

### 信息：issue_mapper 拆分——mapComposeIssues 内联移除，hasErrors 保留

- 位置：`frontend/src/graph/utils/issue_mapper.ts`
- 事实：`mapComposeIssues` 内联进适配层统一方法并移除导出（全代码库零引用，grep 最干净）；`hasErrors` 保留（仍被越界文件 `move_node.ts` / `preview_engine.ts` 消费，不在本次边界内）
- 状态：✅ 已确认（提示词允许二选一，内联方案 grep 验证最干净）

### 信息：graph_store.test.ts 的 sentinel 预置保留

- 位置：`frontend/src/graph/graph_store.test.ts`
- 事实：2 处 `store.lastValidationResult = { valid: false, issues: [] }` 是 missing/corrupted 路径"不覆盖状态"断言的测试脚手架（sentinel 预置），非业务层直写，不在契约清理范围
- 状态：✅ 已确认（测试脚手架，非越界写入）

### 信息：throw 文案约定

- 位置：`useGraphOperationAdapter.ts`（commitToCurrentGraph 空图）、`operation_controller.ts`（COMMON_LAYER_NOT_FOUND）
- 事实：`COMMON_LAYER_NOT_FOUND: 未找到常识层图谱…`（保留原 code 前缀便于 grep）；`commitToCurrentGraph: 当前无 graphView，无法提交操作`（自定义文案，契约只约定改 throw）
- 状态：✅ 已确认

---

## 发现者：@cleaner（08.5 简化期）

### 可改进：issue_mapper.ts 文件名与职责失配 ✅ 已改名（用户确认）

- 位置：`frontend/src/graph/utils/issue_guard.ts`（原 issue_mapper.ts）
- 事实：拆分后文件仅剩 `hasErrors` 判定函数，不再做任何 mapping，"issue_mapper" 名不副实
- 处理：用户确认改名 `issue_guard.ts`——git mv + 3 处 import 同步（move_node / preview_engine / useGraphOperationAdapter）；文件头 JSDoc 已与名称匹配（"ComposeIssue 判定工具" + mapComposeIssues 内联溯源链）
- 状态：✅ 已确认（改名完成，测试全绿）

### 信息：适配层 JSDoc 修正（编排者）

- 位置：`useGraphOperationAdapter.ts` `reportComposeValidation` JSDoc
- 事实：cleaner 指出 JSDoc 以现在时引用已删除的 `mapComposeIssues`，grep 会命中注释形成误导；已改为类型级描述"转换（ComposeIssue → ValidationIssue）"；`issue_mapper.ts` 文件头的溯源链描述保留（构成迁移记录）
- 状态：✅ 已确认

---

## 发现者：@oracle（08.5 验证期）

### 信息：EMPTY_LABEL 的 targetId 是未落图的随机 id（低）

- 位置：`packages/graph-engine/src/core/validate.ts`（EMPTY_LABEL targetId = operation.node.id，`generateNodeId()` 随机生成、未落图）
- 事实：这是 08.5.1 契约明确要求的行为；Graph.vue `canvasErrorIssues` 只消费 severity/code/message（已核实），不按 targetId 定位节点——无实际影响
- 状态：✅ 已确认
  > 记录：若未来画布错误提示按 targetId 高亮节点，需注意该 id 不存在于图中（应做存在性兜底）。

### 可改进：add_node draftNode 保留行为无断言（低）

- 位置：`frontend/src/feature-tools/toolbar/add_node.test.ts`
- 事实：契约要求"空 label 提交后 draftNode 保留（validation.valid false 不清空）"，代码逻辑正确（`if (validation.valid)` 不清空）但无测试断言该行为
- 状态：⏳ 待确认
  > 建议：后续补一条断言防回归（低优先级）。

### 信息：08.5.1"前端零改动"验收口径（合并执行所致）

- 位置：08.5.1 提示词验收标准第 5 条
- 事实：08.5.1/08.5.2 由同一 fixer 会话合并执行，工作区无法独立验证"08.5.1 完成时前端零改动"；以"08.5.1 引擎改动独立且未触碰前端"（git status 无前端新增于引擎改动之外）替代验证口径
- 状态：✅ 已确认

### 不确定：label undefined 加固 ✅ 已确认（结案）

- 位置：`core/validate.ts`（`label.trim()` 无 undefined 守卫）
- 问题：oracle 评估"as 断言绕过类型系统属编程错误，trim() 抛 TypeError 本身也是编程错误暴露，加守卫反而掩盖类型违约；未来引入运行时 schema 校验（如 zod）时再统一加固"
- 状态：✅ 已确认（接受现状结案，不单独加固）
