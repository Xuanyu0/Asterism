# 06-UI层重构

---

## 发现者:@fixer

### 不确定：外部点击规则带来行为变更——画布点击会清空错误通知

* 问题：新外部点击规则下，任意画布点击（含空白画布）都会触发 close() → clearValidationResult()，从而清空画布级校验错误提示（NotificationPanel）。重构前浮空窗仅由 9 个明确关闭点关闭，点击空白画布不会关闭浮空窗。
* 影响：用户点击画布任意位置会同时清掉当前展示的校验错误提示；浮空窗打开时点击空白处即关闭（预期目标），但"清空错误通知"是连带副作用，且触发频率从"明确关闭意图"变为"任意点击"。cleaner 复核：即使当前根本没有浮空窗打开，每次画布点击也会清空 lastValidationResult——提示词将 close() 描述为"幂等无副作用"，实际有清校验副作用。
* 状态：✅ 已确认（修复已实施并经用户验收）
> 建议：pointerdown 监听里仅当 floatingData 非空时才调 close()，避免无浮空窗时误清校验；浮空窗打开时点击外部仍正常关闭。
> 理由：既保留"外部点击关闭浮空窗"的核心语义，又消除"点击画布误清错误通知"的回归。
>
> oracle 裁决：应修复（高优先级，本任务引入的用户可见回归）。已按 cleaner 守卫方案实施：监听首行 `floatingData.value === null` 短路 + close JSDoc 修正 + 新增"无浮空窗时 pointerdown 不触发 clearValidationResult"断言（113 测试全绿）。

### 不确定：mediator.activate 早退路径会先 deactivate 当前 handler

* 位置：`frontend/src/feature-tools/mediator.ts`（activate 内）
* 问题：activate(未注册 id) 时先 deactivate 当前 handler，再发现未注册返回——此时当前 handler 已被取消但 activeToolId/activeHandler 仍指向它，违反接口契约"id 未注册时静默返回，当前状态不变"。
* 影响：实际不可达（所有调用方 GraphModeSelector / GraphPermanentToolbar 均用已注册 id），低严重度。本次变更边界仅允许 mediator 类型收紧，未改动。
* 状态：✅ 已确认（维持单独排期，本次不动）

### 历史遗留：operation_controller.ts 存在未使用的 useUIStore

* 位置：`frontend/src/ui/operation_controller.ts`（import 后从未调用 `uiStore.xxx`）
* 现象：import 的 useUIStore 变量未使用，属历史遗留（该文件为待迁移至 feature-tools/ 的遗留模块）
* 建议：随 operation_controller 迁移时一并清理

### 说明：default_tool.ts 保留 useUIStore 为预期

* 位置：`frontend/src/feature-tools/default_tool.ts`
* 现象：`onNodeDoubleClick` 中的 `requestCanvasFocus`（画布聚焦）仍属 ui_store 保留职责，import 保留非残留
* 状态：✅ 已确认（该职责已随 06.2 迁入 `composables/useCanvasFocus.ts`，本条作废；ui_store 整体删除）

### 说明：测试实现细节（非问题）

* jsdom 环境无 PointerEvent，单测改用 MouseEvent 派发（监听器只读 event.target，行为等价）；组合式函数内 `target instanceof Node` 判型兼作防 `contains(window)` 抛错的加固。
* ref 包裹对象返回 reactive proxy，断言用 toEqual 而非 toBe。

### 说明：切换编辑对象时窗口闪烁（设计代价留档）

* 位置：外部点击规则 + default_tool.onNodeClick 的交互时序
* 现象：窗口开着点击另一节点时，pointerdown 先关窗（DOM 卸载）→ tap 再 open（DOM 重建），存在一次卸载/重挂（输入框焦点丢失）。属"外部点击关闭"设计的固有代价，不影响数据正确性。

### 说明：提示词"幂等无副作用"表述矛盾留档

* 位置：6.1 提示词中 close() 的描述（"幂等无副作用"）与子任务 1 的 close 联动（clearValidationResult）冲突
* 现象：实现遵循了子任务 1 的显式规范，责任在提示词表述。已修正 close JSDoc 明示"清校验是真实副作用"，防止后续维护者误判实现为 bug。

---

## 发现者:@fixer（06.2 画布焦点迁出）

### 不确定：operation_controller.ts JSDoc 残留"可以读取 ui_store"

* 位置：`frontend/src/ui/operation_controller.ts`（L19、L72 JSDoc）
* 现象：ui_store 已删除，JSDoc 中"可以读取 ui_store"为过时错误陈述。06.2 契约边界"仅允许移除 useUIStore import 与实例化，其余代码不动"，故未清理。
* 状态：✅ 已确认（cleaner 已随 06.2 一并清理：删除两处假注释并重排编号，含遗留空行）

### 不确定：CLAUDE.md 文件命名示例残留 ui_store.ts

* 位置：`CLAUDE.md`（"文件命名（snake_case）"小节示例行）
* 现象：示例 `graph_store.ts, ui_store.ts, ...` 中的 ui_store.ts 已不存在。06.2 契约"其余内容与风格保持不动"约束下未动。
* 状态：✅ 已确认（cleaner 已替换为现存文件 `graph_registry.ts`）

### 说明：CLAUDE.md "Pinia (3 个 Store)" 本就过时

* 位置：`CLAUDE.md`
* 现象：原表述"Pinia (3 个 Store)"在改动前即已过时（浮空窗迁出后实际仅 graph_store + ui_store 两个）。06.2 按"数量表述与现状一致"统一为 1 个（graph_store），顺带修正。

### 说明：架构图 useCanvasFocus 放置位置

* 位置：`CLAUDE.md` 架构分层图
* 现象：`composables/useCanvasFocus.ts` 列于"Runtime / UI 状态层 (graph/ + ui/)"段内（替换原 ui_store 行）；该段标题未含 composables/ 前缀，但条目路径与代码实际位置一致，与既有 composables 提法相容。
* 状态：✅ 已确认（无歧义，保持现状）

---

## 发现者:@cleaner（06.2 简化审查）

### 修复：operation_controller.ts 假注释 + 遗留空行

* 位置：`frontend/src/ui/operation_controller.ts`（文件头 + useOperationController JSDoc）
* 现象：两处 `1. 可以读取 ui_store。` 规则在 ui_store 删除后成过时陈述；且原 `const uiStore` 移除后遗留空行。
* 处理：两处删除并重排编号（2→1、3→2、4→3）；删除遗留空行。未触碰任何逻辑。

### 修复：CLAUDE.md 文件命名示例替换

* 位置：`CLAUDE.md`（snake_case 小节示例行）
* 现象：示例 `graph_store.ts, ui_store.ts, ...` 中 ui_store.ts 已不存在。
* 处理：替换为现存文件 `graph_registry.ts`（示例列表保持全部为真实存在的 graph_* 文件）。

### 不确定：CLAUDE.md 示例仍含 graph_operation_types.ts（迁移前遗留）

* 位置：`CLAUDE.md`（snake_case 示例行）
* 现象：`graph_operation_types.ts` 历史上存在、现已不存在，属**迁移前**就存在的残留，超出 06.2 边界未动。
* 状态：⏳ 待确认（建议后续统一修正）

### 说明：生成物残留（project_status.txt / project_report/）

* 位置：`frontend/project_status.txt`、`frontend/project_report/`
* 现象：自动生成的依赖报告仍引用 `ui_store.ts` / `useUIStore`，非源码。
* 状态：✅ 已确认（不属源码，由对应生成工具重新生成时自然更新）

### 说明：测试文件头写文件名（全局惯例）

* 位置：`useCanvasFocus.test.ts` 文件头
* 现象：与 CLAUDE.md"文件头不要写文件名"规则冲突，但**所有**现有测试文件（含 6.1 的 useFloatingWindow.test.ts）均如此，属既有全局惯例，改动反而不一致。
* 状态：✅ 已确认（保持惯例，不单独改新文件）

---

## 发现者:@oracle（06.2 最终验证）

### 不确定：docs/设计/术语映射表.md "UI 存储" 行悬空

* 位置：`docs/设计/术语映射表.md`（L28："UI 存储 → ui_store.ts"）
* 现象：ui_store.ts 已删除，且该行描述（交互模式/选中工具/浮空窗）早已过时。该表是 grep 流程前置加载的 L1 文档，悬空指针有误导性。
* 状态：⏳ 待确认（docs/设计 修改需用户许可）

### 建议：operation_controller.ts import 分组残迹

* 位置：`frontend/src/ui/operation_controller.ts`（import 区）
* 现象：`useGraphStore`（L31）独处一组，与同源 `@/graph/` 的 node_radius/issue_mapper 隔开——ui_store 行移除后遗留。可合并为一个 `@/graph/` 组。
* 状态：⏳ 待确认（低严重度，仅观感）

### 流程备注：06 步骤文档第 2 节状态

* 现象：oracle 验证 11/11 通过、回归风险低，无回流建议。
* 状态：✅ 已确认（步骤文档第 2 节由编排者标记 ⏳→✅ 于用户检查通过后统一处理）

---
