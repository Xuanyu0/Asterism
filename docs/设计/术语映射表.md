# 术语映射表

> 用途：连接中文设计术语与英文代码标识符，保证 grep 检索时搜全所有变体。
>
> 使用方式：在 grep 设计文档或代码前，先查此表获取所有相关标识符，再用 `grep -E "(术语1|术语2|...)"` 一并检索。
>
> **来源标注说明**：
>
> - `设计/` — 概念出自 `docs/设计/`（L1），用户亲手书写
> - `开发文档/` — 概念/实现细节出自 `docs/开发文档/`（L2）
> - `代码` — 标识符/文件名来自实际代码库
> - `设计/ + 代码` — 概念出自设计文档，英文标识符来自代码

---

## 一、前端术语

| 中文           | 英文标识符                                                         | 含义                                                                         | 来源             |
| -------------- | ------------------------------------------------------------------ | ---------------------------------------------------------------------------- | ---------------- |
| 交互模式       | `CognitionTool` / `ArrangementTool`                        | 三种主交互模式，决定画布点击行为的语义                                       | `设计/` + `代码` |
| 认知模式       | `'cognition'`                                                      | 认知演化操作模式，点击节点触发认知操作                                       | `设计/` + `代码` |
| 布局模式       | `'arrangement'`                                                    | 布局操作模式，点击节点选择为操作对象                                         | `设计/` + `代码` |
| 操作控制器     | `operation_controller.ts`                                          | UI 事件路由层，语义事件 → 图操作委托                                         | `代码`           |
| 图交互适配层   | `cytoscape/cy_interaction.ts`                                      | Cytoscape 事件 → 语义事件（`onCanvasClicked` 等回调）                       | `代码`           |
| 渲染器         | `cytoscape/useRenderer.ts`                                         | Cytoscape 实例管理 + 样式配置                                                | `代码`           |
| 图操作翻译层   | `feature-tools/`（各工具 handler）                                  | UI → engine 桥接，逐个暴露认知和布局操作                                     | `代码`           |
| 图存储         | `graph_store.ts`                                                   | GraphData 唯一事实源，`commitBatchToGraphs()` 唯一写入点                     | `代码`           |
| 常驻工具栏     | `GraphPermanentToolbar.vue`                                        | 顶部中央 9 按钮常驻操作栏                                                    | `代码`           |
| 节点浮空窗     | `GraphFloatingWindow.vue`                                          | 节点/边的属性编辑浮空窗                                                      | `代码`           |
| 学习图谱视图   | `Graph.vue`（`views/Graph.vue`）                                   | 页面组合层，挂载 Cytoscape + 协调各组件                                      | `代码`           |
| 常驻操作       | 9 个按钮：add_node ×2 + add_edge ×4 + delete + fold + move_node     | 不依赖模式的永久可见操作                                                     | `设计/` + `代码` |
| 导航卡片       | Navigation Card（Dock / Expand / Hidden 三态）                     | 图谱层级导航 UI                                                              | `设计/`          |
| 预览           | preview（预览布局，确认前临时渲染）                                | 确认前在画布临时渲染位置，不写入 GraphData                                   | `设计/`          |
| 错误反馈       | `lastValidationResult`, `ValidationResult`                         | 操作失败时对用户可见的错误信息                                               | `代码`           |
| 工具中介者     | `feature-tools/mediator.ts`                                        | 全局单例，工具注册、激活/取消、互斥保证、事件转发                            | `代码`           |
| 工具处理者     | `ToolHandler`（`feature-tools/types.ts`）                          | 单个工具的自包含处理器接口                                                   | `代码`           |
| 工具注册表     | `feature-tools/toolbar/config.ts`                                  | 操作按钮的图标、标签、处理器工厂                                             | `代码`           |
| 通知面板       | `NotificationPanel.vue`                                            | 操作错误/提示信息显示                                                        | `代码`           |
| 元素映射器     | `cytoscape/cy_element_mapper.ts`                                   | GraphData → CyElements 渲染投影层                                            | `代码`           |
| 节点质量       | `mass`（`1 + degree`）                                             | 边宽公式中的节点质量参数，`m = 1 + degree`                                   | `设计/`          |
| Cytoscape 样式 | `cytoscape/cy_style.ts`                                            | Cytoscape 实例的节点/边/交互样式配置                                         | `代码`           |

---

## 二、GraphEngine 术语

### 核心概念

| 中文         | 英文标识符                                                                   | 含义                                                                     | 来源             |
| ------------ | ---------------------------------------------------------------------------- | ------------------------------------------------------------------------ | ---------------- |
| 图数据       | `GraphData`                                                                  | 系统的唯一事实源，包含节点列表 + 边列表 + 图元信息                       | `设计/` + `代码` |
| 节点         | `NodeData`（discriminated union）                                            | 图中的知识单元，按 `NodeRole` 分型                                       | `设计/` + `代码` |
| 边           | `EdgeData`                                                                   | 知识间关系的表示，由 kind × direction 确定                               | `设计/` + `代码` |
| 节点角色     | `NodeRole`（`'knowledge'` / `'reference'`）                                  | 节点第一层判别                                                           | `设计/` + `代码` |
| 知识节点     | `KnowledgeNodeData`（`role: 'knowledge'`）                                 | 表达知识的本体                                                           | `设计/` + `代码` |
| 实节点       | `RealNodeForm`（`'atomic'` / `'abstract'`）                                  | 已掌握的知识节点                                                         | `设计/` + `代码` |
| 原子节点     | `'atomic'`                                                                   | 基础实节点，圆形，不可再分                                               | `设计/` + `代码` |
| 抽象节点     | `'abstract'`                                                                 | 拥有子图的实节点，正多边形，边数 = 抽象等级 + 2                          | `设计/` + `代码` |
| 虚节点       | `KnowledgeNodeKind: 'virtual'`                                              | 未掌握的知识节点，圆形，固定半径 r₀                                      | `设计/` + `代码` |
| 引用节点     | `ReferenceNodeKind`（`'communication'` / `'heuristic'`）                     | 原节点在其他图中的代理/投影                                              | `设计/` + `代码` |
| 沟通节点     | `'communication'`                                                            | 子图中联系父图的引用节点                                                 | `设计/` + `代码` |
| 启发节点     | `'heuristic'`                                                                | 通过发散操作创建的跨图引用节点                                           | `设计/` + `代码` |
| 度数         | `degree`                                                                     | 节点连接的边数。决定节点大小（$r = r_0 \cdot \sqrt{1 + \text{degree}}$） | `设计/` + `代码` |
| 认知抽象等级 | `abstractionLevel`                                                           | 内部最大子图层数，系统决定，用户不可改                                   | `设计/` + `代码` |
| 图类型       | `GraphKind`（`'root'` / `'subgraph'` / `'learningBlock'` / `'commonLayer'`） | 图的四种类型                                                             | `设计/` + `代码` |
| 根图谱       | `root`                                                                       | 无父图，整个知识系统的根                                                 | `设计/` + `代码` |
| 子图         | `'subgraph'`                                                                 | 抽象节点内部的展开                                                       | `设计/` + `代码` |
| 知识块       | `'learningBlock'`                                                            | 单轮学习的临时游离产物                                                   | `设计/` + `代码` |
| 常识层       | `'commonLayer'`                                                              | 已内化的知识集合，仅含节点，无边                                         | `设计/` + `代码` |
| 认知状态     | `cognitiveState`                                                             | 折叠/展开标记，持久化为 GraphData 的一部分                               | `设计/` + `代码` |

### 边

| 中文     | 英文标识符                                   | 含义                                     | 来源             |
| -------- | -------------------------------------------- | ---------------------------------------- | ---------------- |
| 有向实边 | `kind: 'real'`, `direction: 'directed'`      | A → B，B 依赖于 A 作为前置知识。禁止成环 | `设计/` + `代码` |
| 有向虚边 | `kind: 'virtual'`, `direction: 'directed'`   | A -·→ B，启发联想。不参与环检测          | `设计/` + `代码` |
| 无向实边 | `kind: 'real'`, `direction: 'undirected'`    | A — B，明确相关，无方向                  | `设计/` + `代码` |
| 无向虚边 | `kind: 'virtual'`, `direction: 'undirected'` | A -·- B，待发掘的关联。唯一非终态边类型  | `设计/` + `代码` |

### 原子操作层（core/）

> 以下标识符来自 `docs/开发文档/开发指南.md`（L2），非 `docs/设计/`（L1）。

| 中文            | 英文标识符                      | 含义                                      | 来源                 |
| --------------- | ------------------------------- | ----------------------------------------- | -------------------- |
| 应用 / 统一入口 | `applyBatch`                       | 统一入口：validate → execute 全流程封装                      | `开发文档/` + `代码` |
| 校验            | `validateOperation`                  | 操作前规则检查，不修改数据                | `开发文档/` + `代码` |
| 执行            | `executeOperation`                   | 给定合法操作，返回新 GraphData            | `开发文档/` + `代码` |
| 批量事务        | `applyBatch`                    | 全量校验 → 全部执行或整批丢弃             | `开发文档/` + `代码` |

### 认知操作层（compose/cognitive/）

| 中文          | 英文标识符                          | 含义                                          | 来源             |
| ------------- | ----------------------------------- | --------------------------------------------- | ---------------- |
| 解构          | `deconstruct`                       | 原子实节点 → 抽象节点 + 空子图                | `设计/` + `代码` |
| 归纳          | `induce`                            | 多节点 → 新抽象节点 + 子图 + 沟通节点/边      | `设计/` + `代码` |
| 内化 / 常识化 | `internalize`                       | 节点从原图转移至常识层                        | `设计/` + `代码` |
| 发散          | `diverge`                           | 跨图创建启发节点 + 有向虚边（三种 case）      | `设计/` + `代码` |
| 探索          | `explore`                           | 开启单轮 AI 学习，产出知识块（MVP 后实现）    | `设计/`          |
| 发掘          | `unearth`, `discover`               | 对虚节点/无向虚边开启学习并替换（MVP 后实现） | `设计/`          |

### 布局操作层（compose/arrangement/）

| 中文     | 英文标识符                                | 含义                          | 来源             |
| -------- | ----------------------------------------- | ----------------------------- | ---------------- |
| 移动     | `moveNode`                          | 单节点拾取放置移动 + 碰撞检测 | `设计/` + `代码` |
| 调整距离 | `adjustDistance`                    | 沿两节点连线方向连续调整边长  | `设计/` + `代码` |
| 调整轨道 | `adjustOrbit`                       | 离散轨道吸附 + 角度调整       | `设计/` + `代码` |
| 环绕布局 | `orbit`                             | 一组节点环绕中心节点排列      | `设计/` + `代码` |
| 路径布局 | `pathLayout`                        | 一组节点沿直线排列            | `设计/` + `代码` |
| 云布局   | `cloud`, `cloudLayout`                    | 节点随机聚集分布（非 MVP）    | `设计/`          |

### 基础设施（infrastructure/）

> 以下标识符来自 `docs/开发文档/开发指南.md`（L2），非 `docs/设计/`（L1）。

| 中文       | 英文标识符                                               | 含义                                      | 来源                 |
| ---------- | -------------------------------------------------------- | ----------------------------------------- | -------------------- |
| 多图注册表 | `GraphRegistry`, `graph_registry.ts`                     | `Map<GraphId, GraphData>`，管理多图上下文 | `开发文档/` + `代码` |
| 搜索       | `searchNodes`, `search.ts`                               | 跨图 label 子串匹配搜索                   | `开发文档/` + `代码` |
| 碰撞检测   | `hasCollisionAt`, `hasCollisionInDrafts`, `collision.ts` | 节点位置冲突的纯几何检测                  | `开发文档/` + `代码` |

### 操作日志与回溯（types/operation_log.ts）

> 回溯设计定案见 `docs/临时文档/GE端逆元问题报告.md`，实现规划见 P3 步骤 09 / 010。

| 中文         | 英文标识符                          | 含义                                                                                                                 | 来源                  |
| ------------ | ----------------------------------- | -------------------------------------------------------------------------------------------------------------------- | --------------------- |
| 原子操作     | `AtomicOperation`, `GraphOperation` | 引擎最小不可分修改单位（add/delete/update/move/collapse/expand/add_graph/delete_graph）                              | `代码`                |
| 批           | `operationBatch`（`OperationBatchItem[]`） | 一次 `commitBatchToGraphs` 调用的全部操作（多图 × 每图多操作）= 一条日志 entry = 一次回溯单元                        | `设计/` + `代码`      |
| 批内项       | `OperationBatchItem`                  | `operationBatch` 的元素 `{ graph, operations }`，按图分组的一组原子操作                                              | `代码`                |
| 日志条目     | `OperationLogEntry`（entry）        | 一批的完整记录：operation（正向）+ reversalOperations（图内逆元）+ graphSignals（图级摘要）+ parentIndex + timestamp | `开发文档/` + `代码`  |
| 图内操作集合 | `ItemOperations`                    | entry 内按图分组的操作（item 的日志形态，graphId 替代运行时 graph 对象）                                             | `代码`                |
| 图级摘要     | `graphSignals`                      | entry 记录图级副作用的元数据：`{ added: GraphId[], deleted: GraphId[] }`，undo 时照摘要反向执行                      | `代码`                |
| 游标         | `cursor`                            | 当前操作日志位置，undo/redo 沿树移动                                                                                 | `设计/` + `开发文档/` |
| 时间跳跃     | `timeLeap`                          | 沿操作日志树路径跳到目标批次（批量 undo + 批量 redo 组合，非重放）                                                   | `设计/`（待规划）     |

---

## 三、项目设计中的 AI 术语

| 中文       | 英文标识符              | 含义                                           | 来源    |
| ---------- | ----------------------- | ---------------------------------------------- | ------- |
| 协作器     | `Collabrator`           | AI 实体统一名称（替代 "Agent"），后端实现      | `设计/` |
| 助手       | `Assistant`             | Human-led + internal 认知源，回答/处理人类问题 | `设计/` |
| 分析师     | `Analyst`               | Human-led + external 认知源，解析外部资料      | `设计/` |
| 提示器     | `Prompter`              | AI-led + internal 认知源，主动分析并提见解     | `设计/` |
| 构建器     | `Builder`               | AI-led + external 认知源，模拟用户构建图谱     | `设计/` |
| 单轮学习   | single learning session | 一次 AI 辅助学习对话的完整过程                 | `设计/` |
| 常识化考验 | common-sense test       | 内化前 AI 生成的验证题集                       | `设计/` |

---

## 四、数据持久化术语

> 持久化概念在 `docs/设计/` 中有提及（localStorage、持久化），但接口定义（`PersistenceAdapter` 等）来自 `docs/开发文档/开发指南.md`（L2）。

| 中文                 | 英文标识符                        | 含义                                 | 来源                 |
| -------------------- | --------------------------------- | ------------------------------------ | -------------------- |
| 持久化适配器         | `PersistenceAdapter`              | 引擎定义的持久化接口，消费者注入实现 | `开发文档/` + `代码` |
| 保存图谱             | `saveGraph`                        | 当前图写入持久化                     | `代码`               |
| 加载图谱             | `loadGraphToView`, `loadGraph`     | 从持久化读取图到当前                 | `代码`               |
| 删除图谱             | `deleteGraph`                      | 从持久化删除图                       | `代码`               |
| 图注册表初始化       | `initRegistry`                    | 扫描 localStorage 重建 GraphRegistry | `代码`               |
| 最后保存时间         | `lastSaveTime`                    | 记录最近一次持久化时间戳             | `代码`               |

---

## 五、本项目其他常用术语

| 中文            | 英文标识符                                                     | 含义                                            | 来源                 |
| --------------- | -------------------------------------------------------------- | ----------------------------------------------- | -------------------- |
| 唯一事实源      | Single Source of Truth, `GraphData`                            | 所有图数据只存在一个地方                        | `设计/`              |
| 单向数据流      | unidirectional data flow                                       | UI → Controller → Engine → Store → Renderer     | `设计/`              |
| Cytoscape 边界  | Cytoscape boundary                                             | cy 不修改 GraphData、不持有引用、不保存业务状态 | `设计/`              |
| 引擎            | GraphEngine, `@my-project/graph-engine`                        | 框架无关的纯函数状态迁移运行时                  | `开发文档/` + `代码` |
| 语义事件        | `onCanvasClicked`, `onNodeClicked`, `onEdgeClicked`（拖拽用 `onDragEnd`） | Cytoscape 原始事件 → 语义化回调                 | `设计/` + `代码`     |
| 学习状态        | learning state                                                 | GraphData 表达的学习者当前认知状态              | `设计/`              |
| 认知演化        | cognitive evolution                                            | 通过认知操作改变知识结构的系统行为              | `设计/`              |
| 位置持久化      | position persistence                                           | GraphData.position 是唯一位置事实源             | `设计/`              |
| 本地优先        | Local First                                                    | MVP 阶段只用 localStorage，不依赖后端           | `设计/`              |

---

## 六、规则分类术语

> 概念体系出自 `docs/设计/05-规则分类.md`（L1）。代码实现分布在 `rule_checkers.ts`、`validate.ts`、`compose/` 等文件中。

| 中文       | 英文标识符      | 含义                                                                                     | 来源             |
| ---------- | --------------- | ---------------------------------------------------------------------------------------- | ---------------- |
| 规则       | rule            | 判断系统状态的约束条件，由分类 × 应用时机 × 触发时机三个维度定义                         | `设计/`          |
| 硬性规则   | hard rule       | 由编程语言/计算机自动保证的规则。                                                        | `设计/`          |
| 自定义规则 | custom rule     | 由产品设计者定义或程序员承诺的规则。                                                     | `设计/`          |
| 全局规则   | global rule     | 针对 GraphData 元素的不变量，一处定义，所有操作自动遵守。                                | `设计/` + `代码` |
| 局部规则   | local rule      | 与特定操作或功能绑定的规则。                                                             | `设计/` + `代码` |
| 内联规则   | inline rule     | 局部规则的子类——硬编码在功能代码中，不需要灵活开关。本质是与该功能逻辑深度耦合的语义预检 | `设计/` + `代码` |
| 非内联规则 | non-inline rule | 局部规则的子类——独立定义且可复用，需要灵活开关或跨操作泛用                               | `设计/` + `代码` |

---
