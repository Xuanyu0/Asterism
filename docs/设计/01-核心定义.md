$$Asterism$$

---

$$ 
Make \space learning \space visible.
$$
---
* **GraphEngine开发目标**：让用户当前的 学习/认知/研究 状态 可视化/可形式化表达
* Antinomy 在管理学习者与知识之间的学习状态。而不是认知/知识

# 节点

* 定义：节点就是知识

## 知识节点

### 两种类型

* **虚节点**：表示学生未掌握/想掌握的知识
* **实节点**：表示学生已经掌握的知识

### 虚节点

* **属性**：标签，度数
* **不可被归纳或者解构**
  * 但可在子图中创建

##### 虚节点上的边

* 虚节点**只能**与无向虚边连接，详见 §边 > 2×2 语义矩阵 > 无向虚边。
* 虚节点**不能作为启发节点**（发散操作的对象限定为实节点，见 §三种交互模式 > Cognition > 发散）。

##### 虚节点的更新

* 虚节点可以通过学生的掌握或直接删除进行更新。
* **掌握虚节点的过程**：
  1. 删除原来的虚节点（及其连接的所有无向虚边）。
  2. 用户利用本轮学习产出的知识块，替代原虚节点的位置，**自主将知识块中的节点与原图谱进行连接**。
  3. 对虚节点原连接的每条无向虚边，用户自行决定处理方式（不变/删除/替换）。
* 对虚节点更新得到的具体内容完全由用户单轮学习得到的见解并最后自己组装决定。

### 实节点

* **属性**：标签，认知抽象等级，摘要，笔记跳转链接，度数
* **可被归纳或者解构**
* 抽象节点作为实节点的子类，拥有实节点的全部属性。单击浮空窗可修改标签/摘要等属性，双击跳转到其内部子图。

## 引用节点

* 包含沟通节点和启发节点
* 启发节点将原知识节点的“摘要”，替换为“上下文摘要”的属性
  * 同样不超过80个中文字符
* 引用节点不是知识的本体，而是知识节点在不同图之间的投影或代理。

| 类型 | 沟通节点 | 启发节点 |
|------|---------|---------|
| **定义** | 子图中特有的节点，用于联系子图和父图 | 已存在节点在其他图内的投影/代理 |
| **创建方式** | 系统自动创建 | 通过 Cognition 模式下的”发散”操作由用户创建 |
| **存在位置** | 只在子图中出现 | 可在任何图中出现 |
| **视觉样式** | 半透明化，几何形状跟随原节点。连接边一端半悬空、逐渐淡化 | 完全复制原知识节点的所有几何属性，无额外视觉处理 |
| **边约束** | 完全继承父图原节点的边属性（kind/direction），与原节点边语义保持一致。**可以连接原节点类型所允许的边** | **只能**通过**有向虚边**连接，不能连接其他边类型 |
| **交互行为** | 双击回到上一层父图 | 双击跳转到对应子图 |
| **标签显示** | 显示子图路径 + 最底层节点标签 | 跟随原节点标签 |
| **与原节点属性一致性** | 实际归属，边，实际位置完全一致 | 标签、摘要等属性与原节点一致；**归属和位置可独立于原节点** |

### 引用节点约束

* **原则**：引用节点与原节点在知识语义上完全一致。修改引用节点的标签，等价于修改原节点的语义内容——这只是操作便利性，不创造新的语义实体。引用节点仅作为原节点在不同位置出现的代理。
* 区分：
  * **沟通节点**：边属性在子图下与原节点完全一致，是严格镜像。仅为视觉效果与操作便利设计。
  * **启发节点**：标签、度数属性与原节点一致，但**归属和位置可独立于原节点**，同时可以独立修改“上下文摘要”的属性。

| 操作分类 | 对启发节点是否允许 | 原由 |
|---------|------------------|------|
| 解构 | ❌ | 改变原节点抽象等级，修改语义本质 |
| 内化 | ❌ | 改变原节点存在状态 |
| 归纳 | ✅ | 仅改变启发节点归属子图 |
| 发散 | ✅ | 仅新增有向虚边，改变代理的连接 |
| Operation 所有操作（删除、添加边等） | ✅ | 按边约束添加或仅移除代理 |
| Arrangement 所有操作 | ✅ | 仅改变位置/布局，不影响语义 |

* 删除定义：
  * 引用节点的删除仅移除代理，不影响原节点
  * 删除沟通节点后，用户仍能通过导航卡片回到上一级
* 原节点约束：
  * 引用节点的属性以及生命周期完全跟随原节点
  * 启发节点有同步修改标签属性和建立无向边的权力
  * 引用节点的度数跟随原节点，连接有向虚边对度数的影响同时作用于原节点和启发节点

## 节点属性
* **名称**：给这个节点起名，直接显示在节点上。（替代原来的标签）
  * 限制：不超过8个中文字符。
* **浮空标签**：概括表示这是什么知识，直接显示在节点下方文字框内。（进一步细化图上可见信息）
  * **限制**：**不超过30个字符**、
  * 一律**支持KaTeX**。
  * 可考虑添加“摘要历史”的功能（非MVP）
* **可被掌握**：用户可通过点击虚节点浮空窗的掌握属性进入单轮学习，结束后更新虚节点
* **认知抽象等级**：认知抽象等级就是内部最大的子图层数。
  * **例如**：初始时节点是**零级抽象**，一级抽象代表一级子图，如三级级抽象节点表示可以最多可以展开三次，得到三级不同的子图
  * **限制**：**用户无法主动修改抽象等级，仅由知识图谱系统决定**
* **摘要**：**严格不能超过80字**，由用户自主编写，凝练地表示知识点的具体内容，**支持轻量 $ Markdown $ 与 $ \KaTeX $**
* **笔记链接**：笔记文件地址，用户点击直达对应笔记（由系统管理）
* **度数**：表示连的边的数量
  * **含义**：度数越大，表示该知识点越不容易遗忘
  * **视觉效果**：**度数越大，节点大小越大**（虚节点除外，虚节点统一采用基准大小 r₀）

## 节点几何样式

### 形状定义

| 节点类型 | 几何形状 | 说明 |
|---------|---------|------|
| 虚节点 | 圆形 | 与实节点形状一致，通过颜色/透明度区分 |
| 实节点 - 原子 | 圆形 | 基础知识单元，圆形表达"完整不可分" |
| 实节点 - 抽象 | **正多边形** | 边数 = 抽象等级 + 2 |
| 沟通节点 | 跟随原节点 | 复制原子节点的几何样式，半透明渲染 |
| 启发节点 | 跟随原节点 | 复制原知识节点的所有几何属性 |

### 多边形朝向

所有抽象节点的正多边形统一**一个顶点朝下**（菱形放）。例如一级抽象（正三角形）一个顶点朝下、二级抽象（正方形）一个对角线顶点朝下。正多边形放置是渲染层样式配置（Cytoscape style），不持久化到 GraphData。

### 大小与度数的映射

节点面积正比于度数（物理隐喻：质量）。半径公式：

$$r = r_0 \cdot \sqrt{1 + \text{degree}}$$

$r_0$ 为孤立节点（degree = 0）的基准半径。degree 翻倍 → 面积翻倍。设有封顶 $r_{\max}$ 防止特大节点抢占视图。**虚节点固定使用 $r_0$，不参与度数映射**。半径定义统一为外接圆半径（圆形 = 几何半径，正多边形 = 中心到顶点距离）。

### 边的粗细

边的粗细正比于两端节点的引力（物理隐喻：二维空间引力反比于距离）：

$$w \propto \frac{m_1 \cdot m_2}{d}$$

其中 $m = 1 + \text{degree}$ 为节点质量，$d$ 为两端节点欧几里得距离。设有最细/最粗封顶。

### 节点交互行为

* 鼠标触碰时**放大**，表示可交互，否则恢复
* **单击**节点弹出浮空窗（显示标签、摘要、笔记链接等详细信息）
* **双击**节点执行跳转（跳转子图 / 跳回父图 / 跳转到原节点所在图）。跳转优先级：引用节点 > 抽象节点（即若节点同时兼具引用和抽象属性，引用语义优先）。

## 子图视觉效果设计

* 用户点击子图展开时，**父节点逐渐放大直到消失，子图从中心扩散开**。（点击”沟通节点”反之）


## 其他想法（也许非MVP）

* 中继节点
  * 定义：仅有浮空标签属性，不限制字数
  * 设计理由：用户可能想提前合并一些边，再从这个中继节点出发连接。或单纯想体现步骤的连续性。并为用户的一些奇思妙想提供支持。
  * 几何样式：极小圆点，且不被度数影响其大小
  * 算法免疫：自动打断“求解循环依赖的算法”
    * 也就是说，中途有中继节点的有向实边环不触发“循环依赖”的判定，也就是被系统认可为有效

---

# 边

* 定义：边就是知识间的关系状态

## 设计理由

* 方向性：代表前置必要知识依赖
* 虚实：代表关系确定与否
  * 实代表确定明晰的关系
  * 虚代表不确定的关系

## 2×2 语义矩阵

边由两个正交轴确定：**本体属性（确定性）（kind）** 和 **方向性（direction）**，形成四种边类型。

||real（实边）|virtual（虚边）|
|---|---|---|
|**directed（有向）**|**有向实边**：A -(作为前置依赖)-> B|**有向虚边**： -·→ 低位知识|
|**undirected（无向）**|**无向实边**：知识 A 相关于知识 B|**无向虚边**：似乎联系于（待发掘）|

### 有向实边

* 认知语义：A -> B，表示 B 依赖于 A（学习顺序依赖或者是作为某种依赖条件）。
* 演化状态：一旦确认，不再转换为其他边。
* 结构约束：仅通过有向实边构成的路径**禁止成环**。

### 有向虚边

* 认知语义：启发。A -·→ B 表示能从A出发联想到B，以此为图谱的不同地方建立连通性
* 演化状态：**终态边类型**，不需要也不会转换为实边。
* 结构约束：**不参与有向实边的环检测**。有向虚边参与的环是合法的。
* 使用时机：只有当两个节点同时已经存在时才能连接。也通过"发散"操作中的第一种/第二种情况创建（伴随启发节点）。

### 无向实边

* 认知语义：A — B 表示两个知识明确相关，且这种关系从哪一头发出都可。
* 演化状态：**终态边类型**。
* 结构约束：无额外结构约束（不参与环检测）。
* 使用时机：只有当两个节点同时已经存在时才能连接。

### 无向虚边

* 认知语义：A -·- B 表示用户猜测两个知识可能有关联，但还不确定具体是什么关系。这是鼓励学生去发现和验证的边类型。
* 演化状态：**唯一的非终态边类型**。用户可通过"发掘"操作（Cognition 模式）开启一轮学习，学习结束后对无向虚边的处理由用户决定：
  * 可保持不变
  * 可删除（确认无关）
  * 可替换为其他边类型（有向实边 / 有向虚边 / 无向实边）
* 结构约束：
  * 虚节点**只能**与无向虚边连接。
  * 单个虚节点可与多条无向虚边连接实节点（探索多个方向的可能关联）。
  * **一个虚节点最多只能与一个虚节点通过无向虚边连接**（防止未知域之间的不可控膨胀——两个不确定的知识之间再叠加不确定的关系，会产生指数级认知混乱，没有任何教学意义）。
  * 详见 §节点 > 虚节点。
* 使用时机：添加虚节点时可附带无向虚边。发散操作创建的是有向虚边（非无向虚边），详见 §三种交互模式 > Cognition > 发散。
* 掌握虚节点时：详见 §节点 > 虚节点 > 虚节点的更新。

## 边的属性

* 任何边都有标签的属性
* **标签**：由用户自定义（可以留空）
  * **限制**：**不超过10个中文字符**，不做其他限制
  * **支持** $\KaTeX$

## 禁止的情况

* **自环**：任何边不得 source === target。
* **重边**：对于任意两个节点（无论知识节点还是引用节点），二者之间**最多只能有一条边**。不允许以不同 kind/direction 在同一对节点间添加多条边。多个引用节点指向同一个知识节点再连接同一个目标节点，同样违反此规则。
* **有向实边成环**：仅在通过有向实边构成的路径中禁止成环（有向虚边参与的环不受限制）。

## 允许的情况

* 允许**孤岛**
* 允许**单个节点被多个节点用有向实边指向**

## 沟通边的视觉样式

沟通边不是一种独立的边概念——它依然属于 2×2 边矩阵的某一格（由 kind × direction 决定）。沟通边的视觉效果（一端半悬空、逐渐淡化）**不由边类型决定**，而是渲染层根据端点节点类型推导：

* 若边的一端连接了沟通节点（`communication`），渲染层将该端渲染为半悬空 + 逐渐淡化
* 视觉行为完全由"端点节点是 communication 节点"这个条件驱动，不需要边层面的 `viewRole` 字段

---

# 图

## 四种类型

* **主图（main）**：学生的全部知识版图，整个知识图谱系统的根。
  * 没有父图。
  * 可包含子图，关联常识层和笔记库。
* **子图（subgraph）**：抽象节点内部的展开，表示知识的深层结构。
  * 必须关联一个拥有该子图的抽象节点（ownerNodeId）。
  * 通过父图 ID（parentGraphId）追溯上层图。
  * 支持多级嵌套（抽象节点的子图内仍可包含抽象节点）。
  * 子图内可出现沟通节点及连接边，用于联系父图。
* **知识块（learningBlock）**：单轮 AI 学习的临时产物，尚未并入主图。
  * AI 可产出知识节点，但只有用户能建立节点间的边。
  * 单轮学习结束后，由用户决定知识块如何组装到原知识图谱中。
  * 导航卡片不存在于知识块中。
* **常识层（commonLayer）**：已内化到无需再记录的知识集合。
  * **仅存在节点，没有任何边**。
  * 节点通过内化（常识化）操作从其他图转移而来。若操作对象为抽象节点，递归转移其下所有子图子树，操作前征求用户确认。
  * 常识化条件：用户需通过 AI 生成的"常识化考验"题集。

## 图与图之间的关系

* 主图是**根**。子图从属于抽象节点。知识块是**游离态**。常识层是**终态**。
* 子图通过沟通节点与父图保持连接。详见 §节点 > 引用节点 > 沟通节点。
* 知识块合并入图谱的路径由用户在单轮学习结束后决定，不是自动合并。

---

# 节点数量控制

## 数量限制

对于**任意**子/父图：
1. 尽量保持在 **50个节点以内**
2. **超过100个节点提醒用户进行抽象**
3. 当前层的知识图谱**超过150个节点拒绝添加**新的节点

## 控制策略

* **抽象**：
  * **设计意图**：让人类主动去抽象提炼相知识，形成一个抽象节点，并且这个节点独立表示一个子图。
  * **功能**：让知识图谱更轻量，AI 分析更快。
  * **抽象的实现**：
    1. 可以把一个实节点转换为抽象节点，并在内部新建空子图（此时抽象节点的**抽象等级为一**）
    2. 可以选取一系列节点归纳成一个新的抽象节点，选取的节点自动成为抽象节点子图的一部分。基于原先已合法的图进行，不新增节点或边，系统仅自动创建沟通节点及连接边（此时抽象节点的**抽象等级取所有被抽象的节点中抽象等级最高的加一**）
* **依赖折叠/展开**：
  * **折叠**：人类可以对于一个 $\text{DAG}$ 上的所有前置依赖节点折叠到该 $\text{DAG}$ 的末尾节点。仅视觉效果，折叠操作不删除图的任何成分
    * **限制**：**必须是纯 $\text{DAG}$，其中的节点不能连有无向边**
    * **功能**：辅助学习的 AI 可以通过分析后继节点的掌握判断那个被折叠的知识节点其实已经被人类掌握了
  * **展开**：**折叠**的逆操作
  
* **常识化**：把节点及其边从原图删除，转移到常识层。若为抽象节点，递归转移其下所有子图子树，操作前征求用户确认。详见 §图 > 四种类型 > 常识层。

---
