* 把常驻工具栏放在屏幕底部，居中对齐
  * 然后提示弹窗就放在屏幕顶部，居中对齐
  * 这样更加符合主流设计
* 认知模式的切换，还是采用工具栏切换的方式
  * 具体的UI形式是底部工具栏左边有一个切换按钮，按TAB键快速顺序切换，具体样式待设计
* 以后这个项目可能自定义的md语法解析
  * 比方说点击超链接会跳转到图上对应的节点，而节点本身就是md文档的载体
* md文档阅读方式和操作日志的阅读方式可能都会采用右侧边栏的形式（是的，永远逃不了的侧边栏设计又回来了）
  * 那么就说明屏幕右侧可能需要有两个互斥的侧边栏展开按钮，这样显然不美观，等待进一步设计
  * 也许可以采用图标加线条的方式？
  * 也就是左边一个图标，表达将要在右侧显示的内容
  * 图标右边，在屏幕边缘“吸附”一个有特定色彩的圆润的线条，表达这将会展开一个侧边栏
* 可以考虑移除常识层，笔记库的设计
  * 笔记库，实际上是一个节点的笔记，可以在同一个页面展示，而不用单独跳转
  * 常识层，本质上是一次特殊的认知搜索操作，可以考虑并入认知模式下的子工具
    * 当然这样设计不严谨，因为认知操作一般改变了图数据，而这个没有改变
    * 所以也许并不是移除常识层的按钮，应该是移位，比方说，把常识层的按钮现实的与根图谱按钮绑定在一起？但我没有具体想明白应该怎么绑定
    * emmm，我发现其实可以把跳转到常识层的按钮放在Dock栏的根图谱图标按钮右边的，这样看起来非常符合直觉！
* 然后就是一个孤零零的设置按钮了，我暂时还没想法。
  * 也许这个可以为未来的图规则设置提供接口？