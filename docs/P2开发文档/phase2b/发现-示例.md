# Phase 2b 发现 — 示例

> 本文件为示例模板。正式的发现文件在对应步骤开始写代码后新建，命名为 `XX-发现-步骤名.md`。
>
> **不确定事项**指开发过程中对已写出的代码的不确定，不是写代码前的待决策事项。待决策事项保留在各步骤文件的「待决设计问题」章节中。

## 历史遗留 BUG

| 发现 | 位置 | 建议修复 | 状态 |
|------|------|---------|------|
| 示例：undo 后折叠状态未清理 | graph_store.ts:120 | 在 revertGraph 中补充 foldState 回滚 | 待修复 |

## 可以改进的地方

- **示例：applyBatchToGraph 参数过多**
  - 位置：graph_store.ts:85
  - 建议：封装为 Options 对象
  - 状态：待处理

## 不确定事项

- **示例：undoStack 快照时机**
  - 描述：当前在 applyBatchToGraph 调用前 pushUndoSnapshot，但跨图操作时多个图同时变更，快照只记录了 currentGraph，不确定是否需要为所有受影响图都做快照
  - 影响：Step 7 Undo/Redo 的实现方式
  - 状态：待确认
