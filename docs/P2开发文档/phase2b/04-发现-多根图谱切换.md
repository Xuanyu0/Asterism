# Phase 2b — Step 04 发现：多根图谱切换

---

## Step 2.0 数据层补丁

### 可改进点（已全部修复）

1. `getGraphById` / `makeLookup` 惰性加载重复 → 提取 `lookupOrLoad` → 最终直接删除（预加载覆盖后冗余）
2. `initRegistry` 双次反序列化 → 改为行内 `kind === 'root'` 检查 → 最终改为 `loadLastActiveRootId` + 预加载
3. `loadGraphToView` 无环检测 → 加 `visited` Set
4. 启动哨兵无 fallback → 加降级创建
5. `loadGraphToView` 失败无用户反馈 → 接入 `lastValidationResult` → NotificationPanel

### 不确定事项

#### 1. `Graph.vue` 直接访问 `graphRegistry` 的 Map API

- 位置：`frontend/src/views/Graph.vue:113-114`
- 问题：直接用 `graphRegistry.size` 和 `.keys().next().value`，绕过 `graph_registry.ts` 的 `listGraphIds`
- 影响：低。当前仅启动哨兵一处使用
- 状态：⏳ 待确认

> 建议：暂不封装。若未来有更多消费者遍历注册表，再统一封装。

#### 2. `listRootGraphIds()` 无调用方

- 位置：`frontend/src/graph/graph_persistence.ts:181`
- 问题：之前被 `initRegistry` 使用，切到 `lastActiveRootId` 后零引用
- 影响：无功能影响（死代码），但导航卡片 UI 的"所有根图谱列表"会需要
- 状态：⏳ 待确认

> 建议：保留至 UI 阶段，不提前删除。

---

## Step 2.4 导航卡片 UI

### 可改进点（低严重度）

#### 1. 面板关闭后 fade timer 不重启

- 位置：`frontend/src/components/GraphNavigationCard.vue:492-499`
- 已修复：`watch(hasOpenPanel)` 的 else 分支重启 fade timer
- 状态：✅ 已修复

#### 2. `goToSegment` 切换图谱时不关闭已打开的面板

- 位置：`frontend/src/components/GraphNavigationCard.vue:307-310`
- 已修复：`goToSegment` 调用 `switchGraphTo` 前加 `closePanels()`
- 状态：✅ 已修复

#### 3. `listRootGraphSummaries` 对每个根图全量反序列化

- 位置：`frontend/src/graph/graph_store.ts:468-478`
- 影响：当前根图量（<10）无感知，图量增长后需优化
- 状态：⏳ 延后处理

#### 4. 导航卡与工具栏同级 z-index

- 位置：`GraphNavigationCard.vue` 与 `GraphOperationToolbar.vue` 均为 z-index 999
- 影响：边缘 UX 冲突，仅当用户主动将卡片拖到工具栏位置时触发
- 状态：⏳ 延后处理

#### 5. `onWindowResize` 无 debounce

- 位置：`frontend/src/components/GraphNavigationCard.vue:539-543`
- 影响：当前 handler 仅做轻量 DOM 读取，无实际性能风险
- 状态：⏳ 延后处理

#### 6. `deleteRootGraphTree` 不检查当前视图根图

- 位置：`frontend/src/graph/graph_store.ts:487-489`
- 已修复：函数入口添加 `graphPath.value[0] === rootId` 守卫，直接返回
- 状态：✅ 已修复

#### 7. `RootGraphSummary.updatedAt` 字段零消费

- 位置：`frontend/src/graph/graph_store.ts:81` 定义 + `frontend/src/components/GraphNavigationCard.vue` 模板
- 影响：无功能影响，但字段声明 + 提取两处维护成本持续存在
- 状态：⏳ 延后处理

### 不确定事项

#### 1. `icon-asterism.svg` 的 `?component` 导入方式

- 位置：`frontend/src/components/GraphNavigationCard.vue:48`
- 问题：`import iconAsterism from '@/assets/icon-asterism.svg?component'` 依赖 Vite 的 SVG 组件导入插件。实测 `tsc --noEmit` 未报错。
- 状态：✅ 已确认（构建环境已配置）

#### 2. 搜索输入框 `focus()` 的 nextTick 竞态

- 位置：`frontend/src/components/GraphNavigationCard.vue:426-428`
- 问题：`searchInputElement?.focus()` 在 `nextTick` 后调用，依赖于 search panel 的 `v-if` 渲染完成。
- 状态：✅ 已确认（Vue 同步刷新机制下无实际风险）

---

## Step 2.5 双击节点跳转子图

### 可改进点（低严重度）

#### 8. `switchGraphTo` 清理三元组重复

- 位置：`Graph.vue:207-210` 与 `GraphNavigationCard.vue:304-308`
- 现象：`closeFloatingWindow + deactivate + loadGraphToView` 在两个文件中有相同实现。
- 状态：⏳ 延后处理

> 建议：提取到 `operation_controller.ts` 作为 `switchGraphTo(targetGraphId)` 共享方法。

#### 9. 导航目标判断逻辑内联过长

- 位置：`Graph.vue:184-205`
- 现象：22 行 if/else 链（引用→抽象→父图→空操作）内联在事件回调中。
- 状态：⏳ 延后处理

> 建议：提取为 `resolveDoubleClickTarget(nodeId, graphView)` 纯函数，放在 `operation_controller.ts` 或 `graph_store.ts`。
