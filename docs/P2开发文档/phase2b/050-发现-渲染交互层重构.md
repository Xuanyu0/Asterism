# Step 1 — 发现：mapper 清理 + 目录迁移 + 样式补充

> 执行期间由 @simplifier + @oracle 审查发现，编排者复核后归档。

---

## Step 1：执行过程发现

### 改进 I-1：6 个未使用的类型 import（已修复）

位置：`frontend/src/cytoscape/graph_element_mapper.ts`（原 import 语句）

现象：`CyNodeData` / `CyEdgeData` 精简后，`EdgeDirection`、`EdgeKind`、`NodeRole`、`KnowledgeNodeKind`、`ReferenceNodeKind`、`RealNodeForm` 不再被任何类型注解引用。

> 建议：删除即可。已修复。

状态：✅ 已确认（修复于同一会话）

### 改进 I-2：`getCyInstance` 的 @example 注释函数名不同步（已修复）

位置：`frontend/src/cytoscape/use_cytoscape_renderer.ts:47`

现象：`@example` 中写 `const cy = getCy()`，但实际导出函数名是 `getCyInstance`。

> 建议：修正。已修复。

状态：✅ 已确认（修复于同一会话）

### 改进 I-3：术语"投影"未完全清除（已修复）

位置：`cytoscape/` 下多个文件

现象：mapper 的主要注释已更新，但 `CyEdgeElement`、`CyElements`、`mapNodeToCyElement`、`mapEdgeToCyElement` 的 JSDoc 以及 `use_cytoscape_renderer.ts` 的 3 处注释仍残留"投影"。

> 建议：全部替换为"映射"。已修复。

状态：✅ 已确认（修复于同一会话）

### 改进 I-4：graph_interaction.ts destroy() 未被消费

位置：`frontend/src/cytoscape/graph_interaction.ts` — `bindCyEvents` 返回 `{ destroy() }`

现象：`Graph.vue` 中 `bindCyEvents` 的返回值未被捕获，`destroy()` 从未被调用。在当前实现下无内存泄露——`cy.destroy()` 随 `renderer.destroy()` 清理所有事件。但如果未来需要热替换事件绑定而不销毁 cy 实例，需要补上调用。

> 建议：当前无需处理。严重度低。

状态：⏳ 待确认

### 改进 I-5：`move_node.ts` 仍用 `.style('opacity')`，未切换到 `.move-picked` class

位置：`frontend/src/feature-tools/toolbar/move_node.ts:166,314,375`

现象：`cytoscape_style.ts` 已定义 `.move-picked` class，但 move_node 仍使用内联样式。

> 建议：Step 2 迁移时切换为 `addNodeClass('move-picked', 'move')`。属于 Step 2 的工作范围。

状态：⏳ 待确认（Step 2 处理）

---

## @oracle 审查发现

### 信息 O-1：Step 1 存在 Scope Guard 越权修改

位置：`frontend/src/cytoscape/use_cytoscape_renderer.ts`

现象：提示词明确禁止修改该文件（Step 2 工作），但实际 diff 含 4 行注释/文档更新（术语替换 + 路径迁移 + @example 注释修正）。

> 建议：不回流——改动属于 Step 1 术语统一 + 路径迁移的自然延伸，且为注释级无功能性影响。后续执行应更严格遵守 Scope Guard 边界。

状态：✅ 已确认（不回流）

### 信息 O-2：fix-1（dblclick 提取）与 Step 1 的 diff 混合

位置：`Graph.vue`、`default.ts`、`mediator.ts`、`types.ts`

现象：fix-1（onNodeDoubleClick 从 Graph.vue 提取到 default.ts）的改动与 Step 1 的 import 路径迁移混在同一批未提交变更中，无法从 git 历史区分。

> 建议：Step 2 执行前先 commit 当前变更（Step 1 + fix-1），保持原子提交历史。

状态：⏳ 待确认
