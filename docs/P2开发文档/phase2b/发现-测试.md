# Phase 2b — 发现：测试

> 本文件为工作流测试产物，记录标题格式统一任务期间的发现。

## 标题格式统一任务期间的发现

### 改进：步骤标题缺状态标记

位置：`docs/P2开发文档/phase2b/` 下 01-09 步骤文件

现象：SKILL.md 要求步骤标题行标记 `✅` / `⏳`，未标记默认 Pending。当前 9 个步骤文件中，仅 Step 0 和 Step 1（含子步骤 1.1/1.2/1.5）有标记，其余 Step -1、2、3、4、5、6、7 均无状态标记。

影响：会话启动协议无法机械判断这些步骤的进度，需人工读内容推测。

> 建议：逐步骤评估实际完成状态后补标记。此为内容决策，需用户确认后写入对应步骤文档。

状态：待确认

### 改进："待决设计问题"与"待解决设计问题"术语混用

位置：
- `04-步骤-多根图谱切换.md:39` — `### 待决设计问题`
- `05-步骤-共依赖.md:39` — `### 待决设计问题`
- `05-步骤-共依赖.md:49` — `### 3.4 待解决设计问题：选中状态归属`

现象：同一类子步骤标题，有的用"待决"，有的用"待解决"。

影响：grep 检索时漏匹配，术语不统一。

> 建议：统一为"待决设计问题"（更简洁）。需用户确认。

状态：待确认

### 改进：H1 步骤名与 H2 步骤名不一致

位置：
- `03-步骤-规则系统.md` — H1 `Step 1：规则系统` vs H2 `Step 1：规则系统架构定位与前端入口规划`
- `06-步骤-认知操作.md` — H1 `Step 4：Cognition` vs H2 `Step 4：Cognition 操作实现`
- `07-步骤-布局操作.md` — H1 `Step 5：Arrangement` vs H2 `Step 5：Arrangement 操作实现`
- `08-步骤-错误反馈.md` — H1 `Step 6：错误反馈` vs H2 `Step 6：错误反馈链路`

现象：同一步骤在文件标题（H1）和步骤标题（H2）中名称不同。

影响：文件名、H1、H2 三处名称不一致，检索和引用时产生歧义。

> 建议：H2 步骤名与 H1 保持一致，额外描述移至 H2 副标题（`—` 之后）或 blockquote。此为内容决策，需用户确认。

状态：待确认
