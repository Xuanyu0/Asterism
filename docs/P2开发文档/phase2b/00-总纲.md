# Phase 2b 步骤规划 — 功能收尾 → MVP 交付

## 总体方向

1. **首要任务**：实现多根图谱切换（保存/加载/切换 UI），让用户能管理多个独立知识图谱。此功能对前端的实际使用体验最为关键。
2. **核心功能**：落实 Cognition（induce / internalize 多选 UI、diverge 跨图搜索）和 Arrangement（moveNode 拖拽、orbit / path 布局、adjust 连续调整）全部操作的前端端到端链路。
3. **共依赖优先**：先开发 Cognition 和 Arrangement 共同依赖的基础能力（多选机制、交互模式框架），再分别实现各操作。
4. **Undo / Redo 延后**，置于 Phase 2b 末尾。
5. **文档层级继承**：任何新功能的加入，必须先在设计文档（`docs/设计/`）完成概要设计，再拟定对应开发文档（`docs/开发文档/`），如有必要可写 spec（`docs/spec/`）。始终确保下级文档中的功能设计概念在上游文档中有明确继承来源。

## 开发范式指导

| 范式 | 核心主张 | 本项目落地 |
|------|---------|-----------|
| 逻辑层与表现层分离 | 逻辑层可不启动引擎/UI 独立运行测试 | GraphEngine 框架无关纯函数包，119 测试直接跑；Cytoscape 只读投影 |
| 30 秒反馈循环 | 改一行代码到看见效果不超过 30 秒 | Vite HMR < 1 秒；UI 组件/交互逻辑/视觉参数分离布置 |
| 先想清楚反馈再写代码 | 动手前明确完整 UI 交互流程 | Step 1 先定义全部操作的交互流程规范再写代码 |
| 部分验证先行 | 用最小成本检验设计假设 | compose 函数支持 dryRun；Step 1 交互架构设计即纸上原型 |

## 开发步骤

| # | 文件 | 步骤 | 状态 | 难度 |
|---|------|------|------|------|
| 01 | [01-步骤-设计文档补完](01-步骤-设计文档补完.md) | 设计文档补完 | | 🟢/🟢/2处 |
| 02 | [02-步骤-架构调整](02-步骤-架构调整.md) | GraphRegistry 归入 Runtime | ✅ | 🟢/🟢/7子任务 |
| 03 | [03-步骤-规则系统](03-步骤-规则系统.md) | 规则系统架构定位与前端入口规划 | 🔨 | 🟡/🟡/7子步骤 |
| 04 | [04-步骤-多根图谱切换](04-步骤-多根图谱切换.md) | 保存/加载/切换 UI | | 🟢/🟢/200行 |
| 05 | [05-步骤-共依赖](05-步骤-共依赖.md) | 多选机制 + 拖拽基础设施 | | 🟡/🟢/150行 |
| 06 | [06-步骤-认知操作](06-步骤-认知操作.md) | Cognition 操作实现 | | 🟡/🟡/400行 |
| 07 | [07-步骤-布局操作](07-步骤-布局操作.md) | Arrangement 操作实现 | | 🟡/🟡/500行 |
| 08 | [08-步骤-错误反馈](08-步骤-错误反馈.md) | 错误反馈链路 | | 🟢/🟢/100行 |
| 09 | [09-步骤-操作回溯](09-步骤-操作回溯.md) | Undo / Redo | | 🔴/🔴/300行 |

## 重要决策

### 已确认

- **applyBatch 作为唯一入口**：`pipeline.ts` 新增 Phase 3 全局规则校验；`applyOperation` 已彻底删除
- **全局规则表**：新建 `core/checkers/global_rules_table.ts`，所有全局规则签名统一为 `(graph) => ValidationIssue[]`
- **Registry 修改统一经过 apply 入口**：除 `initRegistry`（启动重建）和 `loadGraphToView`（加载时注册）外，所有 registry 写操作由 `applyBatchToGraph` / `applyBatchToGraphs` 统一完成
- **Undo / Redo 统一在 Step 7 解决**：此前步骤不引入 OperationLog 的局部补丁，继续用 `undoStack` + `pushUndoSnapshot` 做最小可用实现
- **选中状态暂留 graph_store**：方案 A 在 Phase 2b 可接受，若未来 UI 需要复杂反馈再迁移到 `ui_store`

### 待确认

- 见各步骤文件中的「待决设计问题」章节

## Phase 2b 完成标志 = MVP 交付

1. **多根图谱切换**：用户可保存当前图谱、列出所有已保存图谱、切换到另一个图谱；启动时自动加载上次使用的图谱
2. **Cognition 操作**：用户可通过 deconstruct / induce / internalize / diverge 改变知识结构
3. **Arrangement 操作**：用户可拖拽移动节点、使用 orbit / path 布局、adjust 连续微调
4. **操作失败可见**：用户在所有操作失败时能看到错误提示
5. **Undo / Redo**：用户可回溯操作，按钮在边界时灰显
6. **explore / unearth 保持 TODO**（Phase 4 AI Runtime）
7. **Cloud Layout / Knowledge Group 保持 TODO**（非 MVP）
